# LibraMAS G10 剩余功能闭环与大屏验收记录

日期：2026-08-21  
分支：`codex/g1-runnable-skeleton`  
审查代码提交：`f670b88adb1d315fb22969daa5c2f6e795a8054a`

## 1. 本轮完成结果

- Agent Workspace 已成为全局协作层，Orchestrator 与 8 个业务 Agent 跨路由保持，右侧工作栏可查看状态分布、角色、目标、工具、证据、任务转交、Directive、数据源和真实事件。
- Guest Workspace 仍完全驻留内存；Demo 审计实现了不可变事件事实、Directive 状态事实、UUIDv5 幂等、容量上限、失败保留队列和默认关闭的四重启用门槛。
- 审计迁移只包含两个 `CREATE TABLE IF NOT EXISTS` 和一个 `INSERT IGNORE` 迁移标记；执行器拒绝 `UPDATE`、`DELETE`、`DROP`、`TRUNCATE`、`REPLACE`、`ALTER`、`RENAME` 以及非白名单表。
- 推荐结果解码已由布尔失败改为路径级严格错误。真实问题来自合法公开字段 `evidence: null` 被旧解码器误判；现在仅兼容契约允许的 nullable/optional 字段，非法字段仍会给出安全的字段路径、期望类型和实际公开类型。
- 既有推荐任务幂等回放恢复已持久化 `agent_actions`，事件明确携带 `replayed=true` 和“历史真实动作回放”语义，不触发新的 Agent dispatch、LLM、审计或业务写入。
- 蓝白设计系统、12 列栅格、自然页面滚动、统一字体/间距/状态卡及 ECharts 懒加载已经落地；旧样式和旧组件保留为兼容资源，没有删除。

## 2. 页面与真实数据

- 探索首页：真实馆藏 KPI、分类占比与排行、知识图谱预览、主题入口、Agent 策略摘要和带来源/观测/过期时间的 `EXTERNAL_DEMO` 情境。
- 智能推荐：查询控制与真实阶段分离，结果独占下一行；展示结果数、平均置信度、可用数、主通道、负反馈摘要、完整书名/解释以及 MySQL/Graph/Vector 分组条。
- 知识图谱：筛选/统计、ECharts 主图、节点详情三栏工作台；真实验收返回 60 个节点、68 条关系、7 个类型，并明确局部截断边界。
- 阅读路径：无结果时展示真实三阶段分组规则；有结果时显示入门、进阶、深化的数量、平均难度、可用量、顺序、依据及详情/图谱入口。
- 馆藏洞察：真实 14,989 个资源、14,986 本图书；Treemap、可用率环形图、出版年代柱状图、热门主题气泡图和推荐通道分组条共 5 类可视化，不构造趋势、同比或借阅热度。

## 3. 验收证据

- 后端：`609` 项 Python 测试全部通过。
- 前端：`60` 项 Vitest 测试全部通过。
- 生产构建：安全不可覆盖构建成功；页面和 ECharts 路由代码已拆分，无单一图表框架重复依赖。
- 浏览器尺寸：`1920×1080`、`3840×2160`、`1366×768`、`1280×720`、`390×844` 均无文档级横向溢出；移动端可见触控项不小于 42px，Agent 快捷项实际为 52px；推荐页不再使用固定 360px 高度。
- 真实只读运行：Overview、Graph、Resource、Workspace SSE 和既有推荐幂等回放均已验证；Neo4j 局部查询的偶发超时会显示真实错误/重试入口，不使用伪造图。
- G10 dry-run：11 条 Workspace 事件、5 条 Directive 状态事实、1 条迁移标记，最大增量 17；数据库连接/写入、DeepSeek 请求、Neo4j/Chroma 写入、文件删除和数据库物理删除均为 0。
- MySQL 只读对账：`agent_workspace_event` 与 `interaction_directive_fact` 当前均不存在，指定 Workspace 计数均为 0。

## 4. Successor ChangePlan（尚未执行）

原阶段 5 计划没有绑定完整执行器，不能用于数据库执行。新计划绑定已审查提交和七个关键输入文件的 SHA-256：

- `plan_id`: `0cb5fb89-ba1a-5f8d-ab32-841b2d4df6e5`
- `plan_hash`: `534f66b511d0fd9cc17d4aa625f8a5310d0d592711cf001a6bdcf1bb83e16333`
- 环境：`mysql://127.0.0.1:62306/recpro`
- 允许动作：创建 2 张新表；追加 11 条事件事实、5 条 Directive 状态事实和 1 条迁移标记。
- 最大新增行：17。
- 明确为 0：DeepSeek、外部网络、Neo4j/Chroma 写、Outbox claim、容器/数据卷变更、文件删除、数据库物理删除和现有行覆盖。

只有用户精确批准以上 successor `plan_id + plan_hash` 后才能建立数据库连接并执行。批准前，Stage 5 的数据库审计验收仍是唯一待执行项；系统其他读者侧核心功能、全局 8 Agent 协作、真实只读数据和既有推荐结果解析已可运行。

## 5. 安全边界

- 本轮文件删除、数据库物理删除、容器删除和数据卷删除均为 0。
- 未执行数据库迁移，未新增推荐事实，未发起新的 DeepSeek 请求。
- 失败采用 fail-forward：保留已经创建的结构或追加事实、关闭审计开关并报告；绝不通过删除做补偿。
- 任何超出 17 行预算的写入、新 LLM 请求、容器替换或数据修订都必须生成新的精确 ChangePlan。
