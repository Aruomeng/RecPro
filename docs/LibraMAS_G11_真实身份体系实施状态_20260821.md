# LibraMAS G11 真实身份体系实施状态

## 已完成且可独立测试

- Identity & Access bounded context：账号、凭据、会话、刷新令牌、角色、权限、一次性码、安全事件和个性化授权。
- Argon2id 密码、带 Pepper 的标识/令牌摘要、版本化 HS256 JWT、刷新令牌轮换、重放撤销、登录失败锁定。
- 正式登录、激活、刷新、退出、修改/重置密码、账号管理、角色动作和个性化授权 API。
- Bearer 身份贯通推荐、阅读路径、Agent Workspace、SSE、曝光、反馈和行为接口；访客不发送伪造用户身份。
- 蓝白大屏登录弹窗、账号菜单、授权向导、5 分钟无操作退出和研究演示入口隔离。
- 12 张 IAM 表、3 个有效状态视图和 MySQL Adapter 已实现，但尚未执行迁移。

## 验证证据

- 后端 G11：20 项测试通过。
- 前端：65 项测试通过，`vue-tsc` 与安全追加式 Vite 生产构建通过。
- G11 dry-run：19 条白名单 SQL、最大追加 37 行；数据库连接/写入、真实账号、DeepSeek、Neo4j、Chroma、删除均为 0。
- Git 提交：`e75ea9f`、`674f270`、`b23f359`、`31e4fa4`、`26c36a8`。

## 等待精确批准的数据库计划

- `plan_id`: `8e9d0b95-3dac-5ceb-87ed-7838deb8b667`
- `plan_hash`: `aa1f06a0e4cc4975c8f45b76189464cd22148b5eb641b2420184c633195c2441`
- 目标：仅在 `mysql://127.0.0.1:62306/recpro` 创建 12 张新表、3 个视图并追加 37 条固定角色/权限/迁移事实。
- 不包含：引导管理员、真实读者、凭据、会话、授权事实、推荐事实或 DeepSeek 请求。

只有用户精确批准以上未变化的 `plan_id + plan_hash` 后，执行器才允许首次数据库连接。批准迁移之后，仍需为“引导管理员创建”和“真实登录—画像—推荐闭环验收”分别生成新的精确 ChangePlan；这两步不能复用当前 37 行迁移预算。

### 最小权限数据库账号计划（必须在 Schema 计划之后执行）

- `plan_id`: `634a97dd-8177-5e7d-ad98-4a400f1c7d1f`
- `plan_hash`: `b62b4d266c7109d40e99d790a382c22b23e3f321e79f7884511c8840562b9c7e`
- 目标：创建一个 `recpro_identity` MySQL 登录并追加 29 条表级权限事实。
- 允许：仅 IAM 白名单表和视图上的必要 `SELECT`、`INSERT`、`UPDATE`。
- 禁止：`DELETE`、`DROP`、`ALTER`、Schema `CREATE`、`GRANT OPTION` 以及任何非 IAM 表访问。
- 业务数据写入、DeepSeek、Neo4j、Chroma、文件/容器/卷变更均为 0。

该计划当前也未执行。执行前本机受保护环境需要存在 MySQL root 凭据和一个至少 20 字符的新身份账号密码；两项当前均未写入 `.env.host`，执行器会因此 fail-closed。不得用 root 或迁移账号作为身份服务运行账号。

## 不变安全边界

- 不删除文件、数据库行、数据库、容器或数据卷。
- 不修改历史画像、行为、推荐或图谱事实。
- MySQL DDL 可能隐式提交；若发生异常，只保留已创建对象并禁用身份组合根，采用前向修复，不做补偿删除。
- 初始管理员没有默认密码；原始读者证号、密码、刷新令牌和一次性码不写日志。

## 2026-08-26 首次批准执行结果

- 原 Schema 计划通过全部静态校验并开始执行。
- 12 张 IAM 基础表创建成功；MySQL 随即拒绝 `recpro_migrator` 的第一条 `CREATE VIEW`。
- 执行器立即停止。只读对账确认：12 张表均存在且各为 0 行；3 个 IAM 视图均不存在；固定角色、权限、授权事实和 G11 迁移标记均为 0。
- 没有执行删除、回滚删除、表重建、数据修改、真实账号创建、最小权限账号计划或 DeepSeek 请求。

由于 MySQL DDL 隐式提交，原计划不能复用。新的前向 successor 仅保留既有空表并补齐缺失对象：

- `plan_id`: `061bf0c9-a232-53bf-a338-e92635a9b677`
- `plan_hash`: `1fa4c998ecfe379f2008fe4afdb677630a5df6e58bc90c76f97bbcfe495eb78a`
- 新增范围：3 个视图和 37 条固定角色/权限/迁移事实。
- 执行身份：受保护的本地 MySQL root 凭据，因为 migrator 缺少 `CREATE VIEW`。
- 删除、覆盖、真实用户、会话、DeepSeek、Neo4j 和 Chroma 预算仍全部为 0。

必须先精确批准此 successor。执行成功后，原最小权限数据库账号计划 `634a97dd-8177-5e7d-ad98-4a400f1c7d1f` 才能继续。

### 2026-08-26 successor 执行与运行账号状态

- Schema successor 已成功：3 个视图已创建，37 条固定事实已追加并精确对账，删除为 0。
- 最小权限账号原计划在客户端 SQL 格式化阶段停止；通配主机 `%` 未转义，MySQL 未收到 `CREATE USER`。
- 只读对账确认 `recpro_identity@%` 账号为 0、IAM 授权事实为 0。
- 修复已增加回归测试；因执行器输入哈希改变，原账号计划不可复用。

新的账号 successor：

- `plan_id`: `d67a05b2-4a4e-58d8-8a06-b34c1d4cae75`
- `plan_hash`: `7a6ad99a35abf778595cd4f6c6868b7f7e8a1fa175b67d308845b74e32aa31c7`
- 目标仍严格不变：1 个 `recpro_identity@%` 账号、29 条 IAM 表级 `SELECT/INSERT/UPDATE` 权限，业务行写入与删除均为 0。

### 2026-08-26 最终执行结果

- 账号 successor 已成功：创建 `recpro_identity@%`，权限精确为 `SELECT=15`、`INSERT=9`、`UPDATE=5`。
- Schema 级权限为 0；没有 `DELETE`、`DROP`、`ALTER`、`CREATE` 或 `GRANT OPTION`。
- 同一计划幂等重放返回 `changes=0`。
- `.env.host` 和 `.env.identity.local` 权限均为 `0600` 且被 Git 忽略；JWT、标识 Pepper、令牌 Pepper 和数据库密码均未进入日志或验收回执。
- Workbench 已以正式认证配置启动：前端 `5173`、后端 `8000`；OpenAPI 已挂载登录、刷新、退出、当前用户、激活和个性化授权接口。
- readiness 为 `READY`，MySQL、Neo4j、Chroma、LLM、推荐管线、交互管线和配置包均为 `UP`，`can_recommend=true`。
- G11 共 25 项测试通过。
- 当前真实读者和管理员账号仍为 0；下一步必须通过独立引导账号 ChangePlan 创建首个研究管理员，不允许默认密码或直接复用合成用户 1001/1002。
