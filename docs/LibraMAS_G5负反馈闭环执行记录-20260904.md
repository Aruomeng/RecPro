# LibraMAS 阶段 4：负反馈闭环执行记录（2026-09-04）

## 1. 本次阶段目标

在不删除任何文件、数据库记录、容器或数据卷的前提下，完成一次可追溯的“推荐曝光 → 不感兴趣反馈 → 行为事件 → Profile Outbox Worker → 资源抑制 → 再推荐”闭环。执行对象为合成研究用户 `1001`，不代表真实读者画像结论。

本阶段允许的外部调用仅为已批准推荐计划中的 DeepSeek 推荐请求；反馈、行为、画像重放和再推荐验证均不调用 DeepSeek。

## 2. 已批准的变更边界

### 推荐投影

- `plan_id`：`4899c201-e96d-5c8f-8df1-a5ce0ba5005c`
- `plan_hash`：`9cdbd11155ad4078cda6318ff3525b8bdd8ced4b2d5f7fc4d29fcce9abe62dc4`
- 目标任务：`e2960ee8-5fd0-59a8-933e-b6463be09f11`
- 推荐记录：`66`
- 目标推荐项：`439`，资源：`128`
- 结果：已完成，8 个推荐项，真实 DeepSeek 9 次（Intent 1 次、Explanation 8 次），无回退；推荐项 439 为资源 128《图书馆学情报学论文选集》。

### 反馈与 Worker

- `plan_id`：`bcd255a4-2a91-5743-97f7-591dc870afef`
- `plan_hash`：`7f0fc09d870325ffc71cd06ae34c89fd04882314ce066d399df2d7054e120724`
- 绑定提交：`4f03f0e2000cba106b005dcfd5aa3d28020fd3fa`
- 交互 UUID：
  - 曝光：`410196aa-80a3-5f99-b0c5-c118a10cb009`
  - 反馈：`0b28fabe-6ec1-5441-a253-8e25b33f6f09`
  - 行为：`f592c1b9-3d58-5564-b6ff-d4601fd0359a`
  - 行为会话：`378bd4e6-a54a-52e9-9070-e2923ba6263f`
- 反馈时间窗口：`2030-01-20T15:00:00Z`–`15:01:00Z`。该时间晚于用户已有行为，保证重放顺序确定。

## 3. 实际追加事实

反馈执行器在业务事实和 Worker 事务已经提交后，末尾的固定行数断言发现历史画像重放会补齐既有未记录事件，因此以断言异常退出。没有执行重试、补偿删除或回滚。只读对账确认以下事实已经完整存在：

| 表 | 基线 | 实际增量 | 说明 |
|---|---:|---:|---|
| `recommendation_impression` | 23 | +1 | 确定性曝光 UUID |
| `recommendation_feedback` | 15 | +1 | `NOT_INTERESTED` |
| `user_behavior_event` | 96 | +3 | 曝光、反馈、点击 |
| `profile_update_outbox` | 42 | +2 | 两条 Worker 任务，均 `DONE` |
| `user_resource_state` | 12 | +1 | 资源 128 为 `HIDDEN` |
| `profile_replay_run` | 45 | +2 | 反馈和点击两个 as-of 快照 |
| `profile_change_log` | 80 | +19 | 首次完整 as-of 重放补齐历史日志 |
| `user_interest_tag` | 41 | +13 | 完整重放后的正向兴趣投影键 |
| `user_negative_preference` | 28 | +2 | 目标主题两个负偏好键 |
| `user_profile` | 3 | +0 | 当前画像行已存在，执行受控更新 |

其余受保护表增量均为 0；Outbox 全局状态为 `DONE=42`、`DEAD=2`，没有 `PENDING` 或 `PROCESSING` 任务。资源 128 的两个主题标签（8506、8521）均产生了负偏好，权重为 `0.447514`，来源事件为反馈事件 136。

首次对账 artifact：

- `artifacts/verification/g5/g8-negative-feedback-reconcile-20260904-001/reconciliation.json`（记录原始断言失败）
- `artifacts/verification/g5/g8-negative-feedback-reconcile-20260904-002/reconciliation.json`（只读重对账，`PARTIAL_APPLY_RECONCILED`）

`PARTIAL_APPLY_RECONCILED` 表示业务链路已完成，但旧计划将可变的全量画像投影误写成固定预算；不是数据库损坏，也不是缺少事实。

## 4. 修复内容

为避免下一次计划低估全量重放的影响，计划构建器现在会在写入前冻结：

1. 用户全部行为事件（最多 5000 条）及其资源标签。
2. 两个 as-of 时间点的完整画像快照。
3. 已存在的 Replay change-log 来源事件集合。
4. 最终兴趣键、负偏好键和需要追加的 change-log 数量。

执行器仅接受非负、与 `replay_projection` 一致的动态预算；固定交互事实仍保持精确 +1/+3/+2 约束。旧计划继续只读兼容，新生成计划必须带完整投影，不能通过放宽校验隐藏后端差异。

## 5. 负反馈效果证据

使用固定评估时刻 `2030-01-20T15:02:00Z` 对真实 MySQL、只读 Neo4j 副本和 Chroma 运行读路径，未发起任何业务写入或模型调用：

- 证据：`artifacts/verification/g4/g8-negative-post-feedback-effect-20300120-001/readonly.json`
- 状态：`PASS`
- 真实候选资源：`68、78、94、194、480、610、616、625`
- 资源 `128` 不再出现在候选集合，且查询断言 `--assert-resource-absent 128` 通过。
- 通道：`MYSQL+VECTOR`；本次没有虚构 Graph 分数。
- MySQL 选定统计表前后计数一致，Chroma 数量保持 `14983`，Neo4j/Chroma 写入均为 0。

该证据证明资源抑制在正确的行为时间之后生效；若以当前 2026 时间评估，2030 行为尚未可见，资源仍可能出现，这是 as-of 语义的预期行为。

## 6. 安全与未完成项

- 文件删除：0。
- 数据库物理删除：0。
- 容器/数据卷删除：0。
- 反馈 Worker 不调用 DeepSeek；本次再推荐只读验证外部模型请求：0。
- Guest 或本阶段未涉及真实账号写入。

本阶段功能闭环已完成，但旧批准计划本身不应标记为 `PASS`，因为它的预算绑定早于全量重放修复。修复提交后，如需再次执行新的反馈写入，必须重新生成 successor ChangePlan，并由用户精确批准；不能复用本计划的写入授权。

下一阶段进入 `demo_degraded`：只读注入 Neo4j、Chroma、LLM 的故障边界，确认系统显示真实降级原因、保持 fail-closed，并继续保留现有事实。
