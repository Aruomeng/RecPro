# LibraMAS 阶段 4：降级路径执行记录（2026-09-04）

## 1. 执行范围

本次只执行 G8 只读故障矩阵，不启动业务写接口、不消费 Outbox、不追加推荐/反馈事实、不调用 DeepSeek，也不改变 Neo4j、Chroma 或 MySQL。故障通过内存适配器和确定性测试注入，覆盖依赖不可用、超时、证据校验失败、重规划边界和历史重放边界。

执行前工作树为 clean，计划绑定当前提交：

- 计划运行标识：`g8-degraded-fault-plan-20260904-001`
- 计划哈希：`11c542cee3c2fa66dd56d5f6c779441c8f4884c69858787ab09d2597655fbaac`
- Git 提交：`dc88ced9af7123b5eed15a5758338c352e79dd49`

## 2. 结果

执行命令对应的只读证据目录：

- `artifacts/verification/g8/g8-degraded-fault-matrix-20260904-001/readonly-fault-matrix.json`
- `artifacts/verification/g8/g8-degraded-fault-matrix-20260904-001/final-runtime-evidence.json`

结果为 `PASS_WITH_BLOCKERS`：

- 17 个只读/故障用例通过。
- 71 项隔离测试通过。
- 8 个追加型用例保持 `PENDING`，没有被伪装成已完成。

已通过的关键边界：

| 用例 | 真实验证内容 |
|---|---|
| A11 | Chroma 超时后 `semantic_score` 为空，任务仍能安全继续 |
| A12 | Neo4j 超时后 `kg_score` 为空，解释不生成虚假路径 |
| A13 | Neo4j 与 Chroma 同时离线时，MySQL 候选仍可安全返回 |
| A14 | MySQL 不可用时返回 503，不从孤立派生索引提供结果 |
| A18 | 阅读路径缺少难度层时返回 `DEGRADED`，不伪造学习阶段 |
| A19 | LLM 输出不存在事实时 Evidence Validator 失败并回退模板 |
| A21 | 命中质量门槛后最多重规划一次，避免无限循环 |
| A25 | 固定 `evaluation_at` 后，未来资源/行为/状态不污染历史重放 |

其余通过项同时验证了冷启动引导、意图保留、分数边界、固定快照确定性、多样性约束、证据引用和输出类型迟滞。

## 3. 安全计数

矩阵证据中的安全计数全部为零：

- 网络请求：0。
- 外部 LLM 请求：0。
- MySQL 读/写：0/0。
- Neo4j 读/写：0/0。
- Chroma 读/写：0/0。
- Outbox claim：0。
- 文件删除：0。
- 数据库物理删除：0。
- Artifact 覆盖：0。

## 4. 尚未执行的用例

以下 8 个用例需要真实追加型事实或受控业务更新，不能由本次只读矩阵代替：

`A02` 请求幂等重放、`A03` 行为幂等重放、`A04` 反馈与 Outbox、`A07` 阅读状态、`A08` 负反馈抑制、`A09/A10` 浏览器曝光边界、`A23` 反馈到画像 Worker。

这些用例必须分别绑定新的精确 ChangePlan（目标 UUID、表增量、DeepSeek 上限、回读断言和回滚/保留策略），并在代码提交变化后重新生成 successor；不得复用历史计划，也不得以“故障测试”名义直接发送业务 POST。

`degraded_dependency_path` 浏览器场景仍处于上述追加型门禁之外的 `PENDING` 状态。当前已完成其核心依赖故障机制验证；要完成浏览器真实场景，还需单独固定测试任务/会话、允许的追加行数和模型请求预算，再申请精确批准。

## 5. 阶段结论

降级边界已达到“可证明安全”的代码级状态：可选通道失败会显式降级，核心 MySQL 不可用会 fail-closed，LLM 证据不合法会回退模板，Agent 不会通过伪造事件维持活跃。剩余工作是受控业务事实的浏览器验收和正式论文实验输入，不是继续扩大故障矩阵。

下一步将优先生成 `degraded_dependency_path` 的只读/追加边界计划草案；在获得精确批准前只做计划校验和浏览器无写入检查。

## 6. 当前 successor 计划

为避免复用旧提交，已在布局修复提交 `083c4d003dc4c9243368efd0be33152e74305529` 上重新冻结六场景 successor（此前 `be23f6e...` 绑定的计划不再作为执行依据）：

- 计划文件：`plans/stage4-browser-successor-stage4-browser-successor-20260905-002.json`
- `plan_id`：`ebeb56c6-5b93-54d0-b05b-b481e23f96ff`
- `plan_hash`：`60b324ba82d8da2d725d6c38503249fac17e17004dc9244dc2511bbbcd6993d9`
- `dependency_degradation` 为 `READ_ONLY_FAULT_INJECTION`，无需业务写入；其余推荐、路径和反馈场景标记为 `BUSINESS_APPEND_REQUIRED`，必须另行生成精确 successor ChangePlan。

## 7. 最新逐场景计划（只读生成）

在当前完整只读快照和 G4 证据基础上，已为仍需浏览器追加验收的三类业务场景生成独立计划；计划只冻结边界，不执行业务 POST 或 DeepSeek：

- 清晰查询：`plan_id=48a27354-b172-5e26-a019-08a092a4d1ba`，`plan_hash=fb6133b56311825b97a02ec8dc6c9a0e8b79e739afeac243fab0a27c9c869034`，最大 MySQL 追加 53 行、DeepSeek 18 次尝试。
- 主题解释：`plan_id=a9f2da9e-a722-57ce-9e34-dfce7a0cf447`，`plan_hash=e698c74cef098f3d666b08ce2efb9ba2c5951b6399cab2e5c9fe266bd24e9f4c`，最大 MySQL 追加 52 行、DeepSeek 18 次尝试。
- 阅读路径：`plan_id=7d8cab40-4b56-5591-a8dd-a7b806d13417`，`plan_hash=7e0684f01c53360baa5d91f6c6d38b1a6cbd85b33dd9fdca3b9540a1692872c6`，最大 MySQL 追加 52 行、DeepSeek 18 次尝试。

三份计划均绑定提交 `ace95dae7b7a3a68031efaf5415b5657f30702ab`；Neo4j、Chroma、Outbox 和删除预算均为 0。未获得每份计划的精确批准前，不执行任何业务写入或模型请求。
