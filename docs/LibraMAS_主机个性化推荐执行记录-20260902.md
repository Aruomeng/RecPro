# LibraMAS 主机个性化推荐执行记录

## 已批准计划

| 项目 | 值 |
| --- | --- |
| plan_id | `5ac30ad8-019b-51f5-90c6-dae8f8baecd0` |
| plan_hash | `ade1046509d5664b5e2b71d89811a660ba0bb2f98bb1b750969ead1a8e1555a6` |
| 用户 | 合成演示用户 `1002` |
| 画像版本 | `4` |
| 任务 ID | `5ba70fff-bc78-5844-887f-a436134f9d75` |
| 推荐记录 ID | `59` |

## 结果

- 首次请求返回 `201`，同一请求的幂等重放返回 `200`，重放未产生新行。
- 任务状态为 `COMPLETED`，并持久化 `profile_version=4`。
- 生成 5 条推荐项；5 条均以 `PROFILE` 为主通道。
- 推荐审计增量：任务 +1、状态转换 +8、候选 +15、记录 +1、结果项 +5、解释 +5、策略决策 +1、追踪 +1。
- DeepSeek、Neo4j、Chroma、行为、反馈与画像更新均为 0；无删除操作。

## 适用范围

此结果证明本地主机 MySQL 管线已能把合成演示用户的持久画像用于实际排序并保留可追溯版本号。当前该组合根仍不包含图谱、向量或真实 LLM 通道；因此不可把本次结果描述为全通道多智能体推荐实验或真实读者实验结果。

## G4 三通道真实 DeepSeek 推荐追加（2026-09-02）

| 项目 | 值 |
| --- | --- |
| plan_id | `fa41a0d9-d107-54e8-86a3-da32e93447a5` |
| plan_hash | `aaa3049d7fa702ede8e924e2dece66eef4ca62e51bf00792a8ed63e41e968500` |
| 代码提交 | `26135ceac3eaf18fed09ef40160801a2a8da9964` |
| 用户 | 合成开发画像 `1002`（无个人数据） |
| 任务 ID | `a2650caa-bc0b-5b8b-a4b4-d83924ab3307` |
| 推荐记录 ID | `60` |
| 图数据面 | 独立最终 Neo4j 只读副本 |

### 结果与对账

- 首次受控 HTTP 推荐返回 `201/COMPLETED`；同一幂等键重放返回 `200`，零新增行且不重复调用模型。
- 7 个业务 Agent 形成一次完整持久化协作链；生成 8 条结果、12 条候选通道事实，实际使用 MySQL、Neo4j 和 Chroma。
- DeepSeek `deepseek-v4-flash` 实际调用 9 次：IntentUnderstandingAgent 1 次、ExplanationAgent 8 次；均在批准上限 18 次内，无回退。
- 每条解释的白名单证据标记均通过校验；不保存原始 Prompt、模型原文、密钥或数据库内部查询。
- 计划允许并实际追加 56 行推荐审计事实：任务 +1、状态转换 +8、候选 +12、记录 +1、结果项 +8、解释 +8、策略决策 +1、追踪 +1、Agent 消息 +7、Agent 结果 +7、制品 +1、编排结果 +1。
- Neo4j/Chroma 写入、物理删除、文件删除和覆盖均为 `0`；只读对账阶段的业务 POST、数据库写入与模型调用也均为 `0`。

### 证据

- 只读三通道基线：`artifacts/verification/g4/g4-final-readonly-fusion-20260902-001/readonly.json`。
- 已批准追加：`artifacts/verification/g4/g4-final-deepseek-projection-apply-20260902-002/g4-recommendation-projection-apply.json`。
- 独立只读对账：`artifacts/verification/g4/g4-final-deepseek-projection-reconcile-20260902-001/reconciliation.json`。

该结果证明在合成开发画像范围内，系统已完成“真实 DeepSeek + 三通道检索 + 8 Agent 协作 + 追加式审计 + 幂等重放”的端到端闭环；它仍不构成真实读者效果或论文确认性实验结论。

## G5 反馈学习与画像闭环（2026-09-02）

| 项目 | 值 |
| --- | --- |
| plan_id | `5b457bbc-8075-512d-a53e-99c1765d0415` |
| plan_hash | `d82ef1ebe61f9c9e749ceb22db897de17068f11dcbcef83fe9925a5bc79b3999` |
| 目标推荐项 | 记录 `60`、项目 `387`、资源 `6322` |
| 用户 | 合成开发画像 `1002` |
| 交互 | 有效曝光、`NOT_INTERESTED/TOPIC_NOT_INTERESTED`、点击行为 |

### 结果与对账

- 受控执行追加/投影变更共 31 项，逐表与批准计划一致；非目标表变化为零。
- 两条新画像 Outbox 均在一次限额为 2 的 Worker 运行中进入 `DONE`，尝试次数均为 1；同一 Worker 再运行返回 0。
- 画像版本从 `4` 递增至 `6`；反馈资源形成 `HIDDEN` 抑制状态，状态版本为 `1`。
- 三个固定 UUID 的第二次写入均返回幂等回放，不会重复追加曝光、反馈、行为或 Outbox 事实。
- 不调用 DeepSeek；Neo4j/Chroma 写入、物理删除、文件删除和覆盖均为 0。
- 反馈后的 G4 三通道只读再推荐通过：7 个推荐流程 Agent、8 个候选，MySQL 与 Chroma 计数不变。

### 证据

- G5 零写基线：`artifacts/verification/g5/g5-final-feedback-readonly-20260902-001/readonly.json`。
- G5 批准执行：`artifacts/verification/g5/g5-final-feedback-apply-20260902-001/g5-feedback-worker-apply.json`。
- G5 独立只读对账：`artifacts/verification/g5/g5-final-feedback-reconcile-20260902-001/reconciliation.json`。

## Stage 2 正式读者认证会话验证（2026-09-02）

- 已批准并执行计划 `3cf9bac6-5b83-5bde-b984-1f79c21596b4`，哈希为
  `e7bd2b7e86b672ae15f05cfa4e97c0feafe0970a929180e182bb034bd1c784e3`。
- 仅使用已有的合成测试读者 `user_id=10001`，完成本地身份 API 的
  `login -> auth/me -> refresh -> logout`；HTTP 状态依次为 `200/200/200/204`。
- 精确 MySQL 追加增量：`iam_auth_session +1`、`iam_refresh_token +2`、
  `iam_security_event +2`。账号状态、角色和个性化授权均保持不变。
- 密码、读者证号、访问令牌和刷新令牌均未输出、写入证据或提交到版本库。
  推荐、反馈、行为、画像、Workspace 审计、Neo4j、Chroma 和 DeepSeek 均为零操作；
  文件、数据库记录、数据库、容器和数据卷的删除均为零。
- 执行证据（受 Git 忽略保护）：
  `artifacts/verification/reader-auth-session/reader-auth-session-20260902-001/acceptance.json`。

## 正式 Bearer 主题推荐与 Agent SSE 验证（2026-09-02）

- 计划 `8168fcc7-e179-51c0-b502-07eaaf547d2f` 的首次执行在 Workspace
  UUID 参数校验处停止。该次仅追加了一个登录会话、一个刷新令牌和一个登录
  安全事件；未创建推荐任务、未发起模型请求，也没有任何删除或补偿操作。
- 修复后，successor 计划 `4bcd0541-5583-546e-a20a-5c455fca32b6`
  （哈希 `78792f7314753a85865b033a1f8c40b74d46f45789fbb00820c416a2cfb1d530`）
  已通过正式 Bearer 身份执行。任务为
  `94fe26ba-0445-542e-bdfc-00544eac22e9`，测试读者为合成账号 `user_id=10001`。
- 公共 API 的 `login`、`auth/me`、Workspace 创建、异步推荐、SSE、状态回读、
  幂等重放和 `logout` 全部成功；SSE 共接收 24 个真实公开事件，返回 8 本图书。
- 精确追加增量：推荐任务 `+1`、状态迁移 `+8`、候选通道记录 `+11`、结果项 `+8`、
  结果解释 `+8`、Agent 消息/结果各 `+7`，以及会话 `+1`、刷新令牌 `+1`、
  安全事件 `+2`。同一请求重放未增加行。
- 本次最终状态为 `DEGRADED_COMPLETED`：IntentUnderstandingAgent 发生
  `LLM_INTENT_FALLBACK` 并采用确定性规则；UserProfile、ResourceSemantic、
  Policy、Recall、Ranking 全部成功，ExplanationAgent 使用 `DEEPSEEK` 成功生成
  证据约束解释。画像版本、角色与授权没有变化；反馈、行为、Outbox、Neo4j、
  Chroma 和 Workspace 审计均为零写入。
- 验收证据（受 Git 忽略保护）：
  `artifacts/verification/authenticated-topic-run/authenticated-topic-20260902-002/acceptance.json`。

## DeepSeek Intent 只读探针（2026-09-02）

- 已批准并执行计划 `20cdabb3-b486-5e2c-86fc-652f6fa24841`，哈希为
  `446a24793d3f43f4920c45c154b5ff23c34045ff295b233c8e1f7838f264528e`。
- 固定非敏感输入“多智能体、知识图谱与智慧图书馆”仅调用一次真实
  `IntentUnderstandingAgent`，结果为 `intent-llm-prompt-v1`、provider=`deepseek`、
  prompt=`intent.classify`，无回退，标准化意图为 `GENERAL_RECOMMENDATION`。
- 数据库、Neo4j、Chroma、Workspace、推荐、反馈、行为、画像和 Outbox 写入均为 0；
  未保存原始模型文本、Prompt、令牌或密钥，文件和数据库删除均为 0。
- 该探针证明 DeepSeek 配置与 Intent Agent 协议均可用；之前主题推荐的
  `LLM_INTENT_FALLBACK` 属于该次运行的瞬时调用失败，系统的降级路径仍保持安全。
- 执行证据（受 Git 忽略保护）：
  `artifacts/verification/deepseek-intent-probe/deepseek-intent-probe-20260902-001/result.json`。
- 反馈后再推荐：`artifacts/verification/g4/g4-final-post-feedback-readonly-20260902-001/readonly.json`。
