# LibraMAS 八阶段实施总状态（2026-09-05）

## 结论

当前代码与本地研究数据面处于 `CORE_LOCAL_READY_WITH_GATES`。馆藏、推荐、图谱、画像、反馈、全局 Agent Workspace、低频规划、降级边界和蓝白前端均有可运行实现；阶段 4 的清晰查询、主题推荐、阅读路径和反馈闭环已按逐场景计划完成受控执行，依赖降级已完成核心只读故障验证，浏览器级降级写场景仍保留门禁。

本记录只更新状态口径，不修改任何历史事实。文件删除、数据库物理删除、容器删除和数据卷删除均为 0。

## 阶段矩阵

| 阶段 | 当前状态 | 已形成的实际产出 | 剩余门禁 |
|---|---|---|---|
| 1. 状态与前端验收 | `PASS` | 五种视口只读验收、蓝白布局、路由和 Workspace 状态统一；探索数据瞬时故障具备有界重试和显式恢复入口 | 真实业务写入不属于本阶段 |
| 2. 登录与画像 | `API_RUNTIME_PASS / GUEST_GATE_PASS / AUTH_BEARER_RECOMMENDATION_PASS / UI_LOGIN_PASS` | IAM Schema、Argon2id、JWT/刷新轮换、角色和授权接口；账号 10000/10001 的登录、刷新、退出、授权和认证推荐证据通过；访客浏览器功能门禁已验证；正式读者 Bearer 推荐闭环和页面级登录—画像回读—退出已按各自批准计划完成 | 个性化授权变更、声明画像编辑、反馈学习和长期画像闭环仍需单独业务 ChangePlan；不在本次 UI 只读验收范围内 |
| 3. Neo4j 只读面 | `REPLICA_READY / COMMUNITY_RBAC_LIMIT` | 独立副本、v1/v2 保留、白名单 MATCH 和有界路径 | 生产绑定和后续图导入需独立计划 |
| 4. 浏览器业务闭环 | `G4_G5_APPEND_PASS / DEGRADED_BROWSER_FIXTURE_PASS` | clear、topic、reading path、negative 已执行；反馈抑制和 17 项依赖故障矩阵通过；隔离浏览器故障夹具已验证 `DEGRADED`、推荐 fail-closed、依赖错误码和无溢出 | 隔离夹具不替代真实依赖进程故障；如需 live fault-injection 浏览器证据，必须另建不连接正式数据的专用组合根；A02/A03/A04/A07/A08/A09/A10/A23 追加型用例保持门禁 |
| 5. 低频后台规划 | `EXPLICIT_PROBE_PASS / DEFAULT_DISABLED` | 预算、去重、脱敏、Directive 白名单；离线自主性验证及 1 次真实 DeepSeek 后台规划探针通过 | 生产或常驻启用需新的模型预算计划；默认仍关闭 |
| 6. 论文实验 | `DEVELOPMENT_READY / FORMAL_INPUT_BLOCKED` | B0–B3、Proposed、消融、A_ONE_HOP_KG、故障矩阵运行器 | 缺正式查询、标注、许可/伦理和冻结 Split |
| 7. OIDC/JWKS | `CODE_COMPLETE / NOT_ENABLED` | 验签、JWKS 缓存、角色映射和 service_worker 浏览器隔离 | 尚未连接真实 IdP 和外部 subject 映射 |
| 8. Compose 生产化 | `POLICY_COMPLETE / DEPLOYMENT_NOT_ENABLED` | fail-closed 门禁、TLS/Secret/备份恢复策略和健康检查模板 | 证书、域名、Secret、恢复演练、生产切换未执行 |

## 本次已完成的阶段性工作

### 负反馈闭环

负反馈计划 `bcd255a4-2a91-5743-97f7-591dc870afef`（hash `7f0fc09d870325ffc71cd06ae34c89fd04882314ce066d399df2d7054e120724`）已完成业务追加和 Worker 处理。资源 128 被置为 `HIDDEN`，两个主题负偏好已写入，两个 Outbox 均为 `DONE`。旧执行器尾部固定预算断言发现全量历史重放会补齐 19 条日志，未执行重试或删除；修复后的构建器已冻结完整重放输入和动态预算。

在固定 `evaluation_at=2030-01-20T15:02:00Z` 的只读再推荐中，资源 128 不再出现在候选集合，MySQL/Neo4j/Chroma 均无写入。证据见：

- `artifacts/verification/g5/g8-negative-feedback-reconcile-20260904-002/reconciliation.json`
- `artifacts/verification/g4/g8-negative-post-feedback-effect-20300120-001/readonly.json`

### 依赖降级矩阵

当前提交 `be23f6e72657bd67ebf7515a3623a82d276f2939` 绑定的计划 `g8-degraded-fault-plan-20260904-001`（hash `11c542cee3c2fa66dd56d5f6c779441c8f4884c69858787ab09d2597655fbaac`）执行了 17 个只读/故障用例，共 71 项断言通过：

- Chroma 超时会移除语义分数并继续；Neo4j 超时不会虚构图谱路径。
- Neo4j 与 Chroma 同时离线时保留 MySQL 候选；MySQL 不可用时 fail-closed 返回 503。
- 阅读路径缺少难度层时显式 `DEGRADED`；非法 LLM 证据回退模板。
- 重规划最多一次，固定时间点历史重放不读取未来事实。

证据见 `artifacts/verification/g8/g8-degraded-fault-matrix-20260904-001/`，安全计数（网络、LLM、数据库、图、向量、Outbox、删除、覆盖）全部为 0。

### 浏览器 successor 边界

为避免复用旧提交，已冻结当前 successor：

- `plan_id`：`680eee0c-5f95-5f3d-8d09-347d41f1230f`
- `plan_hash`：`da71d1393a128563ae55e5edf6c933a4b041e2cadb3fde22908f2fe573717333`
- 文件：`plans/stage4-browser-successor-stage4-browser-successor-20260905-001.json`

其中 `dependency_degradation` 是只读故障注入；推荐、路径、反馈和曝光场景明确要求新的精确 ChangePlan 后才能追加事实或发起模型请求。布局修复后旧 successor 不再使用，当前 successor 为 `ebeb56c6-5b93-54d0-b05b-b481e23f96ff`，hash `60b324ba82d8da2d725d6c38503249fac17e17004dc9244dc2511bbbcd6993d9`，绑定提交 `083c4d003dc4c9243368efd0be33152e74305529`。

在本轮身份文档提交后重新生成的只读 successor 为：

- `plan_id`：`f2bbb316-ebac-57c7-8022-d2a0878a248f`
- `plan_hash`：`011aa06ec0d487f8eeec7ffcb5f90ba330451672c629ba0b8465094e40df4762`
- 文件：`plans/stage4-browser-successor-stage4-browser-successor-20260905-003.json`
- 绑定提交：`d8610d6df3f95d29fe912582185a78e6a38b4e93`

该 successor 仅更新六场景的只读审查边界；聚合预算中的数据库、Neo4j、Chroma、Outbox、DeepSeek、容器、卷和删除操作仍全部为 `0`。其中只有 `dependency_degradation` 可在无需业务写入的前提下执行；其余场景在申请真实运行时必须再生成逐场景精确 ChangePlan。

## 2026-09-05 当前逐场景执行记录

下列执行均使用当时通过的精确 ChangePlan；旧计划在基线变化后被拒绝或废弃，没有重试写入。

| 场景 | 计划与执行 | 实际追加 | 结果 |
|---|---|---:|---|
| 清晰查询推荐 | `ee771208-c4d3-5de1-a2b1-f2d54cfe07ac` / `12f5e926af5f1c1db0990be2e8bc7ec904297f45bf6fabb6e1c21f2a80994e5b`；`stage4-clear-apply-20260905-002` | MySQL 53 行；DeepSeek 9 次 | PASS，任务 `9e008a09-a46b-5a25-a539-723a92bea805`，记录 67，8 项 |
| 主题解释推荐 | `5c9c9dec-7e69-581f-bbde-2b84b5252b6e` / `f76fbb7d654a5cfc75d3811aae26c61be9adc72164381323acce2dfcfcf8f96d`；`stage4-topic-apply-20260905-003` | MySQL 52 行；DeepSeek 9 次 | PASS，任务 `0aa851b4-6380-5d69-ac59-7ef1ad2e7a2c`，记录 68，8 项 |
| 阅读路径 | `19584b89-2dfd-5388-b626-8b2235d062e2` / `e60786e4c776f93dcac836270ff7502fab59646ba1c8a28ee6d330edbffcf3d0`；`stage4-path-apply-20260905-003` | MySQL 52 行；DeepSeek 9 次 | PASS，任务 `0e7a9278-2174-5f74-bd1f-a63252d33ce2`，记录 69，8 项 |
| 反馈与画像 Worker | `df7f67f9-bee2-5c34-a7ff-d4561267377c` / `cbf93913dd6cdb493099ddc49e2eca4298c978370d30acda76c0f3ad5ac913c2`；`stage4-feedback-apply-20260905-006` | MySQL 26 行；Outbox 消费 2 条；DeepSeek 0 次 | PASS，资源 9 置为 `HIDDEN`，负偏好新增 4 条，重放 0 增量 |

本轮四个执行的共同安全结果：Neo4j/Chroma 写入 0，外部 LLM 仅在三个 G4 计划允许的 27 次内调用，非目标表漂移 0，幂等重放新增 0，文件删除、数据库物理删除、容器删除和数据卷删除均为 0。反馈对账见 `artifacts/verification/g5/stage4-feedback-reconcile-20260905-006/`；G4 执行证据分别位于 `artifacts/verification/g4/stage4-{clear,topic,path}-apply-*`。

## 2026-09-05 前端认证与响应式只读验收

本轮只读验收使用当前提交 `629cd99c4727b95619829fd0f7b3596302040469`，未提交表单、未登录、未发送业务 POST，也未调用 DeepSeek：

- 前端单元测试：19 个测试文件、81 个测试全部通过；命令使用固定可用的 Node 25.3 运行时，未改动源码。
- 安全构建：`identity-ui-20260905-001` 完成，`vue-tsc --noEmit` 和 Vite 构建通过；构建目录采用不可覆盖的独立 run ID，未覆盖历史构建。
- 首页、图谱、洞察和全局 Agent 工作栏在 `1920×1080`、`3840×2160`、`1366×768`、`1280×720`、`390×844` 下完成浏览器 DOM 检查：`body.scrollWidth === viewportWidth`，未发现横向溢出或非滚动容器的垂直文字裁切。
- Guest 点击“智能推荐”或“阅读路径”时回到首页并打开登录说明；没有创建推荐任务、反馈事实或画像事实。登录弹窗包含登录、激活账号、忘记密码三个入口，Access/Refresh Token 安全提示可见。
- 全局 Agent 工作栏可收缩/展开；展开后可见 8 个业务 Agent、状态分布、情境数据源和事件时间线。当前 Guest 无活跃 Agent 时如实显示 `0 活跃`，没有伪造工作日志。

环境备注：系统 `/opt/homebrew/bin/node` 指向缺失 `llhttp` 动态库的 Node 25.6，导致直接调用 npm 的一次测试启动失败；随后使用已验证的 Node 25.3 路径完成全部测试和构建。该环境问题不改变应用代码或数据状态。

## 2026-09-05 正式账号浏览器推荐 successor（已执行）

为完成阶段 2 的最后一段真实浏览器链路，已基于当前 MySQL 只读快照生成计划文件 `plans/authenticated-topic-browser-successor-20260905-001.json`：

- `plan_id`：`b198efd2-0bb0-58fe-83d2-2fd6287af507`
- `plan_hash`：`df523bf00f1a573793d1a23eacf9c12d3c75a2eeb0fa943055fab79a487fbac8`
- 绑定基线提交：`669353c6`（执行器允许该提交作为当前祖先，输入文件哈希必须仍匹配）
- 固定身份：正式测试读者 `user_id=10001`，仅使用受保护的 0600 凭据文件；不复用演示用户 1001/1002。
- 计划动作：Bearer 登录、`auth/me`、创建内存 Workspace、一次主题推荐、Fetch-SSE、结果读取、同幂等键重放、退出。
- 预算边界：推荐/任务事实按计划当前基线追加，DeepSeek 最多 18 次；Neo4j、Chroma、反馈、行为、画像、Workspace 审计和额外授权均为 0。
- 安全边界：只允许既定身份会话/推荐追加和登录会话相关状态更新；不执行删除、覆盖、清空、容器或数据卷动作。计划生成本身的数据库写入、DeepSeek 请求和物理删除均为 0。

该计划已获精确批准并完成执行；后续若代码或数据库基线变化，不能复用本计划，必须重新生成 successor。

## 2026-09-05 正式读者 Bearer 推荐闭环执行结果

计划 `b198efd2-0bb0-58fe-83d2-2fd6287af507` / `df523bf00f1a573793d1a23eacf9c12d3c75a2eeb0fa943055fab79a487fbac8` 已获精确批准并执行，证据见 `artifacts/verification/authenticated-topic-run/authenticated-topic-browser-successor-20260905-001/acceptance.json`：

- 账号 `user_id=10001` 完成正式登录（200）、`auth/me`（200）、内存 Workspace 创建（202）、主题推荐创建（202）、Fetch-SSE（200）、状态读取（200）、同幂等键重放（202）和安全退出（204）。
- 推荐任务 `b25ec2bb-3e90-5992-ab0d-52ee167a3b3b` 最终状态为 `COMPLETED`，返回 8 项 `TOPIC_RESOURCES`；7 个实际业务 Agent 均成功且未降级，SSE 共 24 条公开事件，Workspace 回读 40 条事件。
- MySQL 追加与当前状态变化严格落在计划边界：`iam_auth_session +1`、`iam_refresh_token +1`、`iam_security_event +2`；推荐任务/记录/候选/条目/解释/追踪及 Agent 公开事实分别按 `1/1/8/8/8/1/7/7/1/1/1/8` 追加。账号版本与四项既有个性化授权保持不变。
- Neo4j、Chroma、反馈、行为、画像、Outbox 写入均为 0；幂等重放没有额外业务事实或模型重试；凭据值未打印。文件删除、数据库物理删除、容器删除和数据卷删除均为 0。

该执行证明了“真实 Bearer 身份 + 多 Agent 推荐 + SSE + 幂等边界”的后端闭环，不等同于浏览器页面自动填写登录向导；页面级登录/授权仍需单独精确计划和审批。

## 2026-09-05 代码门禁回归

计划预检后执行了无业务副作用的本地门禁：契约测试 67 项、架构测试 28 项、安全测试 36 项、G1 Python 测试 154 项，全部通过。测试中的健康端点与故障分支均为隔离测试请求，不产生数据库、图、向量、模型或审计写入。

## 2026-09-05 探索数据读取恢复

针对浏览器验收中发现的真实问题：后端短暂返回 `EXPLORE_HTTP_503` 时，首页首次读取失败后会一直显示空指标，且图谱缩略图没有恢复入口。本轮仅修改前端内存状态和展示逻辑：

- Exploration 概览和图谱搜索对 `408/425/429/5xx` 及明确网络传输错误最多自动尝试 2 次，单次间隔 350ms；最多允许 3 次，禁止无限轮询。
- `INVALID_EXPLORATION_RESPONSE` 等契约错误不重试，立即显示安全错误摘要，避免用重试掩盖后端契约问题。
- 概览、图谱、路径和资源详情错误相互独立，图谱成功不会清除仍存在的馆藏概览错误。
- 首页在数据不可用时显示“重新读取数据”入口，知识图谱缩略图显示独立重试按钮；洞察页在概览不可用时显示重新读取入口。
- 新增 3 个前端单元测试，覆盖瞬时 503 恢复、契约错误不重试和概览/图谱错误隔离。前端测试现为 20 个文件、84 个用例，全部通过。
- 使用 `identity-ui-20260905-002` 完成 `vue-tsc --noEmit` 和 Vite 安全构建；1280×720 只读浏览器检查显示真实馆藏指标（14,989 项资源、14,986 册图书、62,514 个图节点、145,175 条关系）、60 个局部图节点，且无横向溢出或图表错误覆盖。

本轮未提交登录、推荐、反馈、路径或图谱写操作；数据库、Neo4j、Chroma、DeepSeek、容器、数据卷和物理删除增量均为 0。该修复只改善瞬时只读故障恢复；页面登录门禁随后由下方已批准的 UI successor 完成。

## 2026-09-05 浏览器登录 successor 计划（已批准并执行）

已基于提交 `2cf2a5a4832f751365fe37464ff756617683452d` 和 MySQL 只读基线生成页面级身份验收计划，并在当前祖先提交上完成执行：

- 计划文件：`plans/authenticated-ui-login-successor-20260905-001.json`
- `plan_id`：`97512296-7b04-596a-9fc3-bd6ff2e43416`
- `plan_hash`：`9d92248ef8bbfb666009caf096059b29885d16108e07600865110c60a6829da6`
- 固定身份：正式测试读者 `user_id=10001`；当前账号 `ACTIVE`，四项个性化授权和声明画像已存在，凭据只从受保护的 0600 文件读取。
- 允许动作：浏览器登录、读取 `auth/me` 与声明画像、打开授权面板核对既有状态、退出；不切换授权、不保存画像、不发起推荐、反馈或模型请求。
- 精确上限：`iam_auth_session +1`、`iam_refresh_token +1`、`iam_security_event +2`；只允许登录时间和本次会话注销状态更新。`recommendation_task`、`user_behavior_event`、`user_declared_profile`、`user_personalization_consent_fact` 保持不变。
- DeepSeek、Neo4j、Chroma、推荐/反馈/行为/Workspace 审计、容器、数据卷、覆盖和物理删除均为 0。

该计划已获用户精确批准并执行通过。若代码或数据库基线变化，计划自动失效，后续身份场景必须重新生成 successor。

### 页面级执行结果

证据文件：`artifacts/verification/authenticated-ui-login/authenticated-ui-login-20260905-001/acceptance.json`，状态 `PASS`。

- 浏览器使用正式测试读者 `user_id=10001` 完成登录表单提交，未使用 `X-Demo-User-Id`；顶部显示正式账号、`user` 角色和个性化状态。
- 画像与授权面板成功读取四项既有授权（`DECLARED_PROFILE`、`BEHAVIOR_LEARNING`、`PERSONALIZED_RECOMMENDATION`、`RESEARCH_ANALYTICS`）及声明画像（图书馆学、研究生、多智能体推荐、`zh-CN`）。本次未提交授权撤回、画像保存或任何业务写操作。
- 安全退出完成后返回访客探索入口，登录弹窗关闭，浏览器内认证状态清空。
- MySQL 精确追加：`iam_auth_session +1`（12→13）、`iam_refresh_token +1`（14→15）、`iam_security_event +2`（23→25）。`recommendation_task`、`user_behavior_event`、`user_declared_profile`、`user_personalization_consent_fact` 均保持不变。
- 账号、角色和授权状态除计划允许的登录时间/本次会话注销状态外未发生变化；计划外 DeepSeek、Neo4j、Chroma、推荐、反馈、行为和 Workspace 审计写入均为 `0`。
- 文件删除、数据库物理删除、容器变化和数据卷变化均为 `0`；凭据值未打印或写入证据。

## 2026-09-06 降级依赖浏览器只读验收

为完成下一项低风险浏览器工作，本轮使用一次性隔离的本地 HTTP readiness 夹具，仅向独立前端开发进程返回契约合法的 `DEGRADED` 响应；没有停用、重启或修改真实研究工作台（8000/5173）、MySQL、Neo4j、Chroma 或 DeepSeek。

证据文件：`artifacts/verification/g8/degraded-dependency-browser-20260906-001/readonly-browser-evidence.json`，状态 `PASS`，绑定当前提交 `5a7d2da20c9c2cfcd2f9c3e8305530488d257703`。

- 在 `1280×720` 浏览器中打开系统状态兼容页，后端存活状态仍显示正常；就绪状态显示 `推荐能力不可用`，`can_recommend=false`，并明确呈现 `OPTIONAL_CHANNELS_DEGRADED`。
- Neo4j、Chroma 和 LLM 分别显示不可用及 `NEO4J_TEST_TIMEOUT`、`CHROMA_TEST_TIMEOUT`、`LLM_TEST_TIMEOUT`；后台规划继续显示 `BACKGROUND_PLANNING_DISABLED`。
- 页面显示 fail-closed 说明和兼容边界，`body.scrollWidth=1280`、`body.clientWidth=1280`，无横向溢出；未提交登录、推荐、反馈或其他业务表单。
- 真实 MySQL 只读对账前后均为：`iam_auth_session=13`、`iam_refresh_token=15`、`iam_security_event=25`、`recommendation_task=75`、`user_behavior_event=102`、`user_declared_profile=3`、`user_personalization_consent_fact=6`。
- 本轮真实工作台的 MySQL/Neo4j/Chroma/Workspace 审计/Outbox/DeepSeek 写入均为 `0`；文件、数据库、容器和数据卷删除均为 `0`。隔离 Vite/HTTP 夹具进程在取证后已停止。

该结果证明前端对契约合法的依赖降级能够清晰展示并保持安全待机，不宣称真实依赖进程已经在生产组合根内被故障注入；后者若需要，必须另建不连接正式数据的专用 fault-injection 组合根并单独审批。

## 2026-09-06 `cold_user_guided` 只读预演与 successor

为继续补齐阶段 4 的剩余业务场景，本轮先执行了 `cold_user_guided` 的只读预演，未提交推荐业务请求。证据见 `artifacts/verification/g8/g8-cold-preflight-20260906-001/readonly.json`，状态 `PASS`，绑定当前提交 `49acc9b10aa3ed0051203189be1ce2e4e93ec10c`。

- 固定请求为 `SEARCH_AFTER`：输入“我还不确定要研究什么，先帮我梳理方向”，资源类型 `BOOK/PAPER`，输出 `PERSONALIZED_FEED`，limit 6；用户为合成演示画像 `1001`，本轮不新增账号或画像事实。
- 确定性编排真实 dispatch 了 4 个 Agent：`IntentUnderstandingAgent`、`UserProfileAgent`、`ResourceSemanticAgent`、`RecommendationPolicyAgent`，产生 4 个状态转换；意图规则因输入含糊而安全跳过 LLM，并返回 `LLM_INTENT_SKIPPED_AMBIGUOUS_INPUT`。
- 运行在 `WAITING_CLARIFICATION`，交付策略为 `GUIDED`，返回 2 个澄清问题，没有推荐记录；未伪造候选、Agent 完成或模型响应。
- MySQL 60 张表逐表前后计数一致；MySQL、Neo4j、Chroma、Workspace 审计、Outbox、外部请求和 DeepSeek 请求均为 `0`，文件删除、数据库物理删除、容器删除和数据卷删除均为 `0`。

基于该只读证据和当时的代码提交曾冻结两个 `DRY_RUN` 计划。它们的生成和校验没有连接数据库，也没有执行模型请求；随后本状态文档提交改变了 Git 边界，因此这些文件仅作为审查记录，不能直接执行：

- 六场景聚合审查计划：`plan_id=8ee0ac47-7c2f-579f-93f8-d6954eda88a3`，`plan_hash=7e99073a0a896b620f27160c9d8baf38e06a3e7914e0148966f00769dd077b08`，文件 `artifacts/verification/g8/g8-browser-successor-20260906-001/browser-scenario-plan.json`。
- `cold_user_guided` 精确 successor：`plan_id=c70c89dd-bc66-51db-ad23-5d4ca837777e`，`plan_hash=dcbda44580a31a6bfb0d2cecddacc9384aee2c736f4ebc8431aeb8c510498289`，文件 `artifacts/verification/g8/g8-cold-successor-20260906-001/cold-user-guided-change-plan.json`。

后一个 successor 的原始范围仅允许追加 19 条 MySQL 推荐编排事实（任务 1、状态转换 4、策略/追踪/上下文/澄清/编排结果各 1、公开 Agent 消息/结果各 4、Agent artifact 1）；不包含候选、推荐记录、反馈、行为、画像、Outbox、Neo4j、Chroma、迁移、更新、删除或 DeepSeek 请求。由于提交边界已变化，执行前必须在最终提交上重新生成新的 successor，并精确批准新的 `plan_id + plan_hash`。

## 2026-09-06 `cold_user_guided` 受控执行结果

最终 successor 已绑定提交 `de66b3760001dd9f6fb3f63980976070071436a4`，并在获得精确批准后执行：

- `plan_id=d78332e7-2404-53a8-922d-a128962f6969`，`plan_hash=d0071ff55b21b16d213e905f0a65874cba98d07ca44ad1ef311a5fc2cce59bb3`。
- 执行证据：`artifacts/verification/g8/g8-cold-apply-20260906-001/apply.json`，状态 `PASS`，模式 `APPLY_ONE_BOUNDED_APPEND`。
- 真实任务 `815095d9-77b7-5de7-b39f-cb0a1c768b57`、请求 `b544bf59-e759-5f55-b2e6-7d1036099498`、追踪 `73c8c9fb-b329-53bd-b7e3-7664a3c4db0d` 均与只读预演固定值一致；响应为 HTTP 201、`WAITING_CLARIFICATION`、`context_version=1`、2 个澄清问题、无推荐记录。
- 计划内 MySQL 追加严格为 19 行：任务 1、状态转换 4、策略决策/追踪/上下文/澄清/编排结果各 1、Agent 消息 4、Agent 结果 4、Agent artifact 1。60 张表逐表回读无计划外变化。
- DeepSeek、Neo4j、Chroma、反馈、行为、画像、Outbox 和外部请求均为 `0`；文件删除、数据库物理删除、容器删除和数据卷删除均为 `0`。计划使用 `recpro_runtime@%` 最小权限身份，未打印凭据。

## 2026-09-07 G4 澄清续跑受控执行结果

在完成只读预演、修复续跑计划的动态行数边界并获得精确批准后，已执行澄清答案 `BOOK_AND_PAPER + 多智能体`。证据见 `artifacts/verification/g4/g4-clarification-continuation-apply-20260907-001/g4-clarification-continuation-apply.json`，状态 `PASS`。

- 执行计划：`plan_id=631ce168-bbbf-5b3c-8be2-ad8641749636`，`plan_hash=84f71b248ebbec506948b0e63905746e555a18fd9544a7cc617439b513a77364`，绑定提交 `efe8965254e26689174bd61171686d8cb8f89a70`。
- 原任务 `815095d9-77b7-5de7-b39f-cb0a1c768b57` 和追踪 `73c8c9fb-b329-53bd-b7e3-7664a3c4db0d` 保持不变；追加上下文 v2 后，最新上下文状态为 `COMPLETED`，生成推荐记录 1 条、候选 6 条和推荐项 6 条。
- 计划内 MySQL 追加严格为 47 行：Agent 消息/结果各 7、候选 6、推荐项/解释各 6、状态转换 8，其余策略、追踪修订、上下文、澄清、编排结果、记录和 artifact 各 1。根任务行仍保留 `WAITING_CLARIFICATION/v1`，符合“历史事实不更新、上下文追加为权威”的设计。
- 同一答案幂等键在上下文表中仅出现 1 次；只读回读确认任务归属、答案、6 个推荐项、11 条累计 Agent 消息/结果、2 个 artifact 和 1 条追踪修订均一致。
- DeepSeek、Neo4j、Chroma、反馈、行为、画像、Outbox 和外部请求均为 `0`；文件删除、数据库物理删除、容器删除和数据卷删除均为 `0`。执行使用 `recpro_runtime@%` 最小权限身份，未打印凭据。

## 2026-09-07 G4 图谱证据推荐受控执行结果

在图谱证据只读预演、MySQL 全量基线和计划边界校验完成后，获得精确批准并执行了 G4 图谱证据推荐投影。执行计划为 `plan_id=32a99195-3a0f-58b6-8f3b-4b87b6193d0d`、`plan_hash=f31733cd411f25a8078ac3dbaac63e892b990405ff6ed92f5973413a445baaff`，绑定提交 `6446b253573205a0fa6661e33f878894acf2a251`。

- 执行证据：`artifacts/verification/g4/g4-graph-evidence-apply-20260907-001/g4-recommendation-projection-apply.json`；独立对账：`artifacts/verification/g4/g4-graph-evidence-reconcile-20260907-001/reconciliation.json`。两份证据均为 `PASS`。
- 真实任务 `f3f4d7dc-4576-56c9-b33b-5978543925ef`、推荐记录 `72`、追踪 `0adc1fb4-41fa-5818-9560-40c247efff01` 均成功完成；HTTP `201`，状态 `COMPLETED`，生成 8 个推荐项。
- 计划内 MySQL 追加严格为 `59` 行：任务 1、状态转换 8、候选 15、推荐记录 1、推荐项/解释 8、策略 1、追踪 1、公开 Agent 消息/结果各 7、artifact 1、编排结果 1。执行后全量计数与计划目标一致；同一幂等键重放复用原任务身份且追加 `0` 行。
- 真实编排 dispatch 了 7 个 Agent，候选持久化通道为 `MYSQL + GRAPH + VECTOR`，共 15 条候选（GRAPH 1、MYSQL 8、VECTOR 6）；Chroma 数量前后均为 `14983`，Neo4j 使用 `final-readonly-replica`，图数据库写入为 `0`。
- 本次运行未触发 DeepSeek：配置提供方仍为 `mock`，`IntentUnderstandingAgent` 的 LLM 尝试为 `0`，`ExplanationAgent` 未启用；反馈、行为、画像、Workspace 审计、Outbox、外部请求和其他业务写入均为 `0`。
- 运行态与对账均通过：存活/就绪接口 `200`，推荐能力可用，任务 GET 返回 `COMPLETED`；文件删除、数据库物理删除、容器删除和数据卷删除均为 `0`。

证据口径说明：本次是 **v1 图通道证据** 运行，候选证据引用了 `lib-books-v1-20260810`。当前 v1 合约允许 `GRAPH` 候选没有 `graphpath:` 多跳引用，因此该任务的 1 条 GRAPH 候选 `graph_path_refs` 为空；这不应表述为 v2 多跳路径覆盖率已完成。若后续需要将多跳路径作为推荐硬约束，必须另行生成并批准 successor 计划，切换到 v2 路径证据合约后再执行。

## 2026-09-07 G4 阅读路径受控执行结果

在图谱证据投影完成后，按新的只读基线重新生成并获得批准，执行了阅读路径场景。计划为 `plan_id=966d873e-8a6c-5fea-b8e2-31af1786e763`、`plan_hash=a6493098741bec9f5971729aa41438c4a1215dc06efec27bbd423cf4f246f874`，绑定提交 `fc2840cbd899c6f2e18e2fcb3606db6bf98b00ee`。

- 执行证据：`artifacts/verification/g4/g4-reading-path-apply-20260907-001/g4-recommendation-projection-apply.json`；独立对账：`artifacts/verification/g4/g4-reading-path-reconcile-20260907-001/reconciliation.json`；两份证据均为 `PASS`。
- 真实任务 `22a8cf20-896c-5bc2-a51e-7a581e55f8d8`、推荐记录 `73`、追踪 `3eb6947e-c86b-56cd-bd66-ab381b0e7df7` 完成，输出类型为 `READING_PATH`，上下文版本 `1`，生成 8 个路径条目。
- 计划内 MySQL 追加严格为 `52` 行：任务 1、状态转换 8、候选 8、推荐记录 1、推荐项/解释 8、策略 1、追踪 1、公开 Agent 消息/结果各 7、artifact 1、编排结果 1。全量表回读与目标计数一致；同一幂等键重放追加 `0` 行。
- 该路径运行使用模板解释，DeepSeek 请求为 `0`；Neo4j 仍为 `final-readonly-replica` 且图写入为 `0`，Chroma 前后均为 `14983`，反馈、行为、画像、Workspace 审计、Outbox 和外部请求均为 `0`。
- 只读预检阶段候选通道为 `MYSQL + VECTOR`，最终路径条目的主通道均为 `MYSQL`；本次没有产生合法 `graphpath:` 多跳证据，不能把它表述为 v2 图谱路径推荐验收。若路径推荐必须依赖多跳证据，需另行生成 v2 专用 successor 并增加路径覆盖断言。
- 存活与任务回读通过，文件删除、数据库物理删除、容器删除和数据卷删除均为 `0`。

## 下一阶段顺序

1. 图谱证据与阅读路径的当前追加已完成并对账；下一步按独立 successor 处理新的负反馈或降级写场景，不复用聚合预算。若要求 v2 多跳路径证据，先单独生成对应的图谱 v2 计划和覆盖断言。
2. 若要求真实依赖进程级浏览器故障注入，建立不连接正式数据的专用组合根并生成独立精确 ChangePlan；当前隔离夹具证据已完成。
3. 在正式查询、相关性标注、许可/伦理材料齐备后，执行论文实验输入门禁和可复算实验。
4. 之后再处理 OIDC/JWKS 与 Compose 生产部署。任何数据库、Neo4j、DeepSeek 或基础设施动作均须单独审批。

## 2026-09-05 后台规划探针

阶段 5 的精确计划 `72d5dc22-27d4-5ca1-a3f4-8bd3277e43f3`（hash `0f8639f2c7cb34c9942b9f5f06dc4f945f3ea4d160b8f7867aaa8b3cce778d60`，绑定提交 `271c635258c6a539e4c47438590739c8b8adb5b2`）已获批准并执行。固定匿名 Workspace 只触发 1 次 `deepseek-v4-flash` 请求，返回 1 条经 DirectiveValidator 校验的 `SUGGEST_TOPICS` 建议，置信度 `0.76`，未输出 Prompt 或模型原文。

本次探针的数据库、Neo4j、Chroma、业务 HTTP、Agent 工具调用、Outbox、文件删除、数据库物理删除、容器和数据卷删除均为 0。证据见 `artifacts/verification/background-planning/stage5-background-planning-successor-20260905-001/apply.json`。该结果只证明显式单请求链路可用；后台规划默认开关仍为关闭，不能据此宣称常驻自动规划已启用。

## 历史逐场景追加计划（已由 successor 取代）

基于 2026-09-05 的完整只读快照（60 张表）和真实 G4 召回证据，已生成三份相互独立的追加计划。它们均绑定提交 `ace95dae7b7a3a68031efaf5415b5657f30702ab`，当前只允许审查，不能由聚合 successor 代替执行：

| 场景 | plan_id | plan_hash | MySQL 最大追加 | DeepSeek 最大尝试 |
|---|---|---|---:|---:|
| 清晰查询推荐 | `48a27354-b172-5e26-a019-08a092a4d1ba` | `fb6133b56311825b97a02ec8dc6c9a0e8b79e739afeac243fab0a27c9c869034` | 53 | 18 |
| 主题解释推荐 | `a9f2da9e-a722-57ce-9e34-dfce7a0cf447` | `e698c74cef098f3d666b08ce2efb9ba2c5951b6399cab2e5c9fe266bd24e9f4c` | 52 | 18 |
| 阅读路径 | `7d8cab40-4b56-5591-a8dd-a7b806d13417` | `7e0684f01c53360baa5d91f6c6d38b1a6cbd85b33dd9fdca3b9540a1692872c6` | 52 | 18 |

计划文件位于 `artifacts/verification/g4/stage4-{clear,topic,path}-successor-20260905-001/`，其中每份都固定用户 `1001`、请求/会话 UUID、逐表前置计数和幂等键。三份计划合计上限为 MySQL 157 行、DeepSeek 54 次尝试；Neo4j、Chroma、Outbox、物理删除和覆盖写入均为 0。只有用户分别精确批准对应 `plan_id + plan_hash` 后，才允许按场景执行。

以上表格保留旧的审查记录；当前四个场景的执行状态以“2026-09-05 当前逐场景执行记录”为准。反馈计划生成所需的 `replay_projection` Schema 缺口已在提交 `a49782c` 修复，后续 G5 计划必须绑定该修复之后的提交。

## 版本与安全

本次功能提交：

- `dc88ced`：全量画像重放预算与负反馈对账修复。
- `be23f6e`：G8 降级矩阵记录。
- `a80345c`：浏览器 successor 计划冻结。
- `083c4d0`：修复 1280px 首页 Workspace 摘要竖排并完成多视口回归。
- `ace95da`：冻结最新只读快照并生成三份逐场景追加计划。
- `a49782c`：将 G5 `replay_projection` 纳入公共 ChangePlan Schema，完成反馈 Worker 计划生成与契约校验。
- `3cc2bc72e82eb765946ff8b157f923504a86b73c`：探索概览/图谱只读请求增加有界恢复、独立错误状态和可操作重试入口，并补充 3 个恢复边界测试。
- `2cf2a5a4832f751365fe37464ff756617683452d`：新增页面级正式登录 successor 计划生成器，只读冻结账号、授权和无业务写入边界。

当前分支：`codex/g1-runnable-skeleton`，已推送远端。默认应用仍 fail-closed；后台规划默认关闭；Guest 不产生持久化账号、画像、行为、反馈或 Workspace 审计写入。
