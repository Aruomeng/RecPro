# LibraMAS 合成演示行为追加执行记录

## 目的与边界

本记录仅覆盖为研究演示用户 `1001` 追加确定性合成行为，以便验证推荐、反馈和动态界面链路。数据来自 `SYNTHETIC_DEVELOPMENT_ONLY` 基准，不能用于论文中关于真实读者偏好、推荐准确率或用户满意度的结论。

## 已批准计划

| 项目 | 值 |
| --- | --- |
| 执行时间 | 2026-09-01 |
| plan_id | `faa7889b-737a-54ad-a6e3-50bbc44424f7` |
| plan_hash | `1be7bc93264a0bcc63943f051cd8b561e775564da16db6a3f14441de20ec8de8` |
| 绑定代码提交 | `3cdf6056a6cfd8bd4f82bd6174e6add1167b14a9` |
| 目标 | `recpro.user_behavior_event` |
| 目标用户 | 研究演示用户 `1001`，非 `iam_user_account` 正式账号 |
| 数据集 | `mysql-synthetic-research-20260901-v1` |

## 执行结果

执行器在事务提交前重新核对 16 个事件 UUID、8 个书目映射、目标用户身份及原有行为基线。结果如下：

| 检查项 | 结果 |
| --- | ---: |
| `user_behavior_event` 追加数 | 16 |
| 用户 `1001` 行为总数 | 55 → 71 |
| 16 个固定 UUID | 全部存在且唯一 |
| 合成事件关联 `profile_update_outbox` | 0 |
| MySQL 更新或删除 | 0 |
| Neo4j / Chroma 写入 | 0 / 0 |
| DeepSeek 请求 | 0 |
| 文件、数据库、容器、数据卷删除 | 0 |

只读回读证据位于 `artifacts/verification/synthetic-demo-import/synthetic-demo-import-postapply-20260901-001/reconciliation.json`。该回读按预期将“继续追加”状态标记为 `BLOCKED`，原因是 16 个 UUID 已存在；这正是防止同一计划重复追加的幂等边界。

## 后续约束

这 16 条事实未触发画像更新或 Outbox。若后续要运行反馈学习、画像重算、真实推荐、浏览器闭环或 DeepSeek 请求，必须分别生成新的精确 ChangePlan，并明确新增行数、测试 UUID、模型预算和回读条件。
