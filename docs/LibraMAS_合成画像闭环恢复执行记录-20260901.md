# LibraMAS 合成画像闭环恢复执行记录

## 背景

时间连续的合成画像闭环首次执行时，Worker 初始化未兼容 `.env.host` 的 `RECPRO_MYSQL_PORT` 配置。行为与批量 Outbox 已在此前事务中成功追加，但 Worker 尚未开始；没有执行删除或补偿操作。

恢复代码修复端口键归一化后，只允许消费已存在的 `profile_update_outbox #54`，不允许再次插入行为或 Outbox。

## 已批准恢复计划

| 项目 | 值 |
| --- | --- |
| plan_id | `e49d9f33-8b6b-54da-bbb0-a52d1b4ab868` |
| plan_hash | `b507bd929449a5d4d5773c558ec0b1f3f0753a2daae8c81c27fd5f5af18881aa` |
| 目标 | 仅 `profile_update_outbox #54` |
| 源事件 | `user_behavior_event #122` |
| 目标用户 | 合成演示用户 `1002` |

## 回读结果

| 检查项 | 结果 |
| --- | ---: |
| Outbox #54 | `PENDING/0 → DONE/1` |
| 用户 1002 画像版本 | `3 → 4` |
| 合成行为事实 | 保持 16 条，新增 0 条 |
| 批量 Outbox 事实 | 保持 1 条，新增 0 条 |
| 画像重放记录 | +1 |
| 画像变更日志 | +16 |
| 状态审计事实 | +3 |
| 兴趣标签 | +14 |
| DeepSeek / 推荐任务 / Neo4j / Chroma | 0 / 0 / 0 / 0 |
| 删除、文件删除、容器或数据卷操作 | 0 |

## 说明

Worker 输出的三条重复键诊断对应用户 `1002` 已有的三条历史 `REPLAY` 日志；适配器使用幂等插入，未覆盖或修改已有事实。本次新增 16 条日志均来自新合成行为。后续代码会将该幂等分支改为无告警的存在性检查，避免运行日志产生误导。

该闭环仅为 `SYNTHETIC_DEVELOPMENT_ONLY` 原型演示，不能作为真实读者推荐效果或论文确认性实验结论。
