# LibraMAS 大屏可读性与真实闭环验收（2026-08-14）

## 本轮结论

- 修复前端把合法的 `difficulty_level: null`、`questions: null` 判为 `INVALID_RUN_RESULT` 的契约错误；真实结果现在可进入推荐书架。
- 首页补充推荐工作流、实时指标说明、分类图例和三张高信息量入口卡；全部统计继续来自只读 Exploration API。
- 放大读者界面的导航、标签、输入、Agent、推荐卡和详情文字；Agent 事件区改为 172px 可滚动列表，最多呈现最近 8 条且不再纵向裁切。
- 完成 1920×1080、3840×2160、1366×768、390×844 浏览器检查；四种尺寸均无页面级横向溢出，移动端横向卡片仅在自身容器内滚动。
- 前端 54 项测试全部通过，生产构建目录为 `frontend/dist/kiosk-legibility-20260814-009/`。

## 已批准真实流程

- `plan_id`: `94267c92-247d-52ea-9a4b-d7a8bb32a3e5`
- `plan_hash`: `fe8eee98c6c46eb9a1d646f066308cfb7130964b1e1b2e873edfc9631acd55d3`
- 执行前规范化哈希复核：一致。
- 执行前目标 UUID 查重：2 个 request_id、impression_uuid、feedback_uuid 均为 0 条。

第 1 次推荐产生任务 `43ddf4ce-9e0a-516d-9edc-fd676de69186`：

- 24 条 SSE 事件，序号 1–24 连续；7 个业务 Agent 均有 STARTED/COMPLETED 事件。
- 终态 `DEGRADED_COMPLETED`，原因 `LIMITED_EVIDENCE`，不是 Mock 或接口失败。
- 返回 7 本真实图书；Intent 与 Explanation 均使用 LLM 版本，未出现 `LLM_FALLBACK_USED`。

反馈链：

- 第 1 名资源 14542 的 1000ms 曝光被接受并判定为有效。
- `NOT_INTERESTED` 反馈由 `FeedbackLearningAgent` 处理，资源状态变为 `HIDDEN`。
- 画像更新状态为 `PENDING`；Outbox Worker 未启动、未领取该事实。

第 2 次推荐产生任务 `99633d45-fbbe-5ae2-bdb5-743f853248c8`：

- 24 条 SSE 事件，序号 1–24 连续。
- 被抑制资源 14542 未再次出现。
- 只返回 1 本书并带 `LIMITED_EVIDENCE`；负反馈约束生效，但该复杂查询的候选覆盖仍需后续单独优化。

## 写入与安全核对

执行前后均生成了全表只读快照。实际追加 96 行，低于计划总上限 179 行；资源目录、书目、标签和索引状态表计数均未变化。

主要追加量：

- 推荐任务 2、推荐记录 2、推荐条目 8、条目解释 8。
- Agent 消息 14、Agent 结果 14、Agent 工件 2、编排结果 2。
- 曝光 1、反馈 1、行为事件 2、资源状态 1、Outbox 1。

计划分项估算把 `user_behavior_event` 写成最多 1 行，实测为 2 行，因为有效曝光和负反馈各产生一个行为事实。总追加量仍在计划上限内，但这是需要保留的估算偏差；发现后未再执行任何业务 POST。

本轮实际安全结果：文件删除 0、数据库物理删除 0、MySQL DELETE 0、Neo4j 写入 0、Chroma 写入 0、容器/数据卷删除 0、Outbox 领取 0、验收证据覆盖 0。

详细运行证据保存在本机忽略目录：

- `artifacts/verification/g5/g5-kiosk-approved-preflight-20260814-001/readonly.json`
- `artifacts/verification/g5/g5-kiosk-approved-postflight-20260814-001/readonly.json`
- `artifacts/verification/g8/kiosk-core-acceptance-20260814-001/execution.json`
