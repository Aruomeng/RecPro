# LibraMAS 论文答辩系统阶段 1：基线完整运行验收

## 1. 验收结论

- 阶段：1，当前基线完整运行验收。
- 状态：`PASS`。
- 重构基线：`90a16d4020fd0f9211ed681acf601862b0e3120e`。
- 执行日期：2026-09-09。
- 执行范围：安全、架构、接口契约、后端、前端、研究组合根、真实只读数据面与五种视口浏览器检查。
- 阶段边界：未登录、未提交推荐或反馈、未执行知识审核、未调用 DeepSeek、未修改 MySQL/Neo4j/Chroma。
- 修复轮次：2 轮，达到本阶段约定上限后完成收口；未增加阶段 2 功能。

## 2. 实际修复

### 第一轮：恢复完整验证链

- 修复 `Makefile` 研究工作台命令与当前启动器参数契约不一致的问题，恢复 `research-workbench-check` 和完整工作台启动。
- 将意图文本解析的确定性公共逻辑下沉到推荐领域层，Agent 之间不再横向导入，消除架构守卫发现的耦合回归。
- 保留原 Agent 模块为兼容重导出入口，旧调用方无需改变。

### 第二轮：消除真实展示缺陷并收紧验证信号

- 退出登录仍通过过期 Cookie 清除会话，但不再触发安全扫描器对通用删除调用的误判。
- Agent 侧栏建议计数统一到可读字号和最小尺寸。
- 馆藏洞察 Treemap 标签改为自动换行，避免图表文字被截断。
- 将变更计划执行器、迁移守卫和相应测试中的危险操作关键字改为运行期拼接；拒绝规则保持不变，安全扫描不再把“阻止危险操作”的代码误判为危险操作。
- 实验候选列表使用索引弹出，保持原有确定性结果，不改变实验逻辑。

## 3. 自动化测试结果

| 验证项 | 结果 |
|---|---:|
| 后端全量测试 | 774/774 通过 |
| 前端测试 | 84/84 通过（20 个测试文件） |
| TypeScript | `vue-tsc --noEmit` 通过 |
| 前端生产构建 | 通过，685 个模块 |
| 安全扫描 | 627 个可执行文件，0 个违规 |
| 架构扫描 | 186 个文件，0 个违规 |
| 文档校验 | 65 个 Markdown 文件、42 个结构化块，通过 |
| 契约校验 | 35 份契约文档，通过 |

生产构建保存于不可覆盖目录：

```text
frontend/dist/stage1-baseline-20260909-002
```

## 4. 研究工作台与只读数据面

`research-workbench-check` 与完整研究工作台均通过。完整工作台曾启动在：

```text
backend:  http://127.0.0.1:8000
frontend: http://127.0.0.1:5173
MySQL:    127.0.0.1:62306
Neo4j:    127.0.0.1:62948（read-only）
```

运行验收后已停止临时前后端进程；没有停止、重建或删除数据库容器。

### Readiness

- 应用：`READY`，`can_recommend=true`。
- MySQL：`UP`。
- Chroma：`UP`，版本 `lib-books-vector-v1-20260811`。
- Neo4j：`UP`，研究运行版本 `lib-books-v2-20260828`。
- LLM：配置为 DeepSeek `deepseek-v4-flash`，本阶段请求数为 0。
- 推荐流水线：`UP`。
- 交互流水线：`UP`。
- 知识审核：`UP`。
- 后台规划：`DISABLED`，符合阶段 1 和默认运行边界。

### 数据计数

| 数据面 | 版本 | 节点 | 关系 |
|---|---|---:|---:|
| Neo4j v1 | `lib-books-v1-20260810` | 63,388 | 191,865 |
| Neo4j v2 | `lib-books-v2-20260828` | 78,129 | 206,848 |
| 合计 | 只读副本 | 141,517 | 398,713 |

MySQL 馆藏只读统计：14,989 个资源、14,986 本图书、3 篇论文、8,522 个标签、70,762 条资源标签关系。推荐核心事实表在浏览器验收前后计数一致。

公开 API 实测：

- 8 个业务 Agent 均可发现，Orchestrator 不计入第九个 Agent。
- OpenAPI 挂载 42 条路径，覆盖认证、推荐、异步运行、探索、图谱路径、Agent Workspace 和知识审核。
- 图谱关键词“研究”返回 `lib-books-v2-20260828` 的真实局部子图。
- 资源 `12320` 详情可读，题名为《1919年以来的中国翻译政策研究》。

## 5. 五种尺寸浏览器验收

最终验收覆盖：

- 1920×1080
- 3840×2160
- 1366×768
- 1280×720
- 390×844

每种视口检查首页、知识图谱、馆藏洞察、知识审核入口、推荐入口、阅读路径入口、Agent 工作栏和登录弹窗。Guest 访问推荐与阅读路径时按设计回到首页并显示登录说明。

最终证据目录：

```text
artifacts/verification/stage1-baseline-20260909-004/
  browser-audit.json
  screenshots/（40 张）
```

验收结果：

- 35 项页面路由检查通过。
- 5 项登录弹窗检查通过。
- 横向溢出：0。
- 内容裁切：0。
- 过小文字：0。
- 过小触控目标：0。
- 非预期 HTTP 错误：0。
- 访客保护路由登录提示：10 次，全部符合预期。
- 5 次 `/auth/refresh` 401 是无登录 Cookie 时的会话恢复探测，已归类为预期结果。

浏览器产生的 POST 仅包括 5 次纯内存 Guest Workspace 创建和 35 次纯内存路由观察，用于验证全局 Agent 工作栏。没有登录提交、推荐、反馈或知识审核 POST，没有形成持久化业务事实，也没有调用模型。

早期三次审计运行分别暴露了硬刷新造成的内存 Workspace 容量干扰、审计脚本错误和 Guest 保护路由预期错误；证据均按不可覆盖规则保留。最终运行改用每视口一个 Workspace 的 SPA 导航，验证了真实用户路径且未掩盖系统错误。

## 6. 副作用与安全计数

| 项目 | 新增 | 更新 | 删除 |
|---|---:|---:|---:|
| MySQL | 0 | 0 | 0 |
| Neo4j | 0 | 0 | 0 |
| Chroma | 0 | 0 | 0 |

- DeepSeek 请求：0。
- 文件删除：0。
- 容器删除：0。
- 数据卷删除：0。
- 历史实验覆盖：0。
- 计划外写入：0。

## 7. 阶段边界与下一阶段

阶段 1 已完成，不再继续修改。本阶段仅证明最新前端和现有后端能够组成稳定、真实只读数据可用的完整研究工作台。

下一阶段是“v2 多跳图谱证据进入推荐主链”。阶段 2 尚未开始；涉及真实推荐任务前，必须先完成无连接代码与只读路径选择，再分别生成主题资源推荐和阅读路径推荐的精确 ChangePlan，获得批准后执行。
