# LibraMAS 阶段 1：状态统一与前端只读验收

日期：2026-08-30  
分支：`codex/g1-runnable-skeleton`  
基线提交：`a6e24ce`（前端 Workspace generation isolation）

## 1. 统一后的状态口径

| 目标层级 | 当前状态 | 说明 |
|---|---|---|
| `CORE_LOCAL_READY` | 基本完成 | 8 个业务 Agent + 1 个 Orchestrator、推荐、画像、反馈、Exploration、Workspace、图谱 v2、知识审核、前端蓝白大屏和开发性实验代码均已存在。前端最后一轮生命周期与 Workspace 一致性修复已提交。 |
| `RESEARCH_READY` | 有条件 | G10 Workspace 审计、G12 知识审核和图谱 v2 已追加并只读对账；正式确认性实验仍等待真实任务、相关性标注、许可/伦理材料。研究运行入口仍受 Neo4j Community 最小权限/隔离副本门禁约束。 |
| `PRODUCTION_READY` | 未完成 | 仍需通用 OIDC/JWKS、TLS、密钥托管、备份恢复演练、容量/告警和单机 Compose 生产门禁。 |

历史文档中关于 G10、G11、G12 的早期“待执行”描述以 2026-08-29 的
`docs/LibraMAS_证据约束多智能体推荐实施状态_20260829.md`（文件名实际为
`docs/LibraMAS_证据约束多智能体优化实施状态_20260829.md`）为准；本文件只记录当前验收，不改写历史事实。

## 2. 本阶段代码产出

- 访客直接打开 `/recommend` 或 `/path` 时，路由守卫打开登录说明并返回首页，避免出现空主内容区。
- ECharts 图谱节点的内部 `value` 固定为数值 `1`，显示标签仍由 `name` 提供，避免无障碍摘要把中文标签解析为 `NaN`。
- 保留旧组件、旧样式、旧同步 API 和所有历史事实；本阶段未执行数据库、Neo4j、Chroma、DeepSeek 或业务写入。

## 3. 只读浏览器验收

测试入口：`http://127.0.0.1:5187/`（独立 Vite 端口；不连接业务写入组合根）。

| 视口 | body 宽度 | 横向溢出 | 非滚动容器裁切 | 结果 |
|---:|---:|---:|---:|---|
| 1920×1080 | 1920 | 0 | 0 | PASS |
| 3840×2160 | 3840 | 0 | 0 | PASS |
| 1366×768 | 1366 | 0 | 0 | PASS |
| 1280×720 | 1280 | 0 | 0 | PASS |
| 390×844 | 390 | 0 | 0 | PASS |

已检查首页、知识图谱、馆藏洞察、系统状态和受保护推荐/路径入口。推荐/路径在
Guest 模式正确显示登录对话框；直接访问不会发送推荐或反馈业务请求。图谱摘要
不再出现 `NaN`，正文和入口均可读。

## 4. 机器验证

- `vue-tsc --noEmit`：PASS。
- 前端 Vitest：19 个测试文件、81 个测试 PASS。
- 只读浏览器布局检查：5 种视口 PASS。
- 当前工作树在提交前仅包含本阶段两处前端修复和本交接文档。
- 文件删除：0；数据库物理删除：0；容器/数据卷删除：0；数据库写入：0；DeepSeek 请求：0。

## 5. 仍需按计划推进的工作

1. 重新只读核对 IAM 当前账号、首个管理员和运行账号状态；若需要真实账号或密码状态变更，单独生成 successor ChangePlan。
2. 生成并审批隔离 Neo4j 只读副本/图谱数据面计划；不接管或修改既有图库。
3. 生成最新六场浏览器业务闭环 successor ChangePlan；背景规划在场景验收期间保持关闭，避免模型预算泄漏。
4. 在代码门禁完成后实现低频后台规划（每会话最多 3 次、间隔至少 10 分钟、设备每日最多 12 次）。
5. 收到真实标注、许可和伦理材料后再执行正式论文实验；开发代理数据不得替代正式结论。
6. 增加 OIDC/JWKS 适配和单机 Compose 生产安全配置。

上述任何数据库写入、新容器/数据卷、真实 DeepSeek 请求或生产密钥操作，均需新的精确
`plan_id + plan_hash` 和明确批准；本阶段不预批准这些动作。
