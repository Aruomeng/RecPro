# LibraMAS 阶段 2：真实登录与个性化闭环验收（2026-08-31）

## 结论

阶段 2 已完成。真实本地账号 `user_id=10001` 已完成激活、正式 Bearer 登录、个性化授权、行为学习撤回、声明画像写入、刷新令牌轮换和退出验证。该账号是非真实个人的合成测试身份，不复用历史演示用户 1001/1002。

本阶段执行严格使用最终授权计划：

- `plan_id`: `29e67e3e-2dcc-5e88-a7f9-6e5381cc1b0d`
- `plan_hash`: `1e105a6d325714df50395560f30a21217a6836a66ac2c0c7a653de5446d3a708`
- 运行标识：`stage2-reader-grant-recovery-20260831`
- 代码边界：`954645c0b0a774d75e953e39baa38e80ac8e1479`

## 实际产出

- `recpro_identity` 获得且仅获得两组画像表最小权限：
  - `user_declared_profile`: `SELECT, INSERT, UPDATE`
  - `user_declared_profile_history`: `SELECT, INSERT`
- 账号状态：`ACTIVE`，`auth_version=2`，`role_version=1`，`must_change_password=false`。
- 四个授权范围的最终状态：
  - `DECLARED_PROFILE=GRANT`
  - `BEHAVIOR_LEARNING=WITHDRAW`
  - `PERSONALIZED_RECOMMENDATION=GRANT`
  - `RESEARCH_ANALYTICS=GRANT`
- 声明画像投影已验证为：图书馆学、研究生、多智能体推荐、zh-CN，版本 1。
- 访问令牌、刷新令牌轮换、读者退出和管理员退出 HTTP 状态均符合契约：`200/204`。
- 原先两次中断产生的旧会话和历史事实全部保留；未执行删除、回滚或补偿。

## 数据库对账

以下为阶段完成后的只读计数；历史基线中的推荐任务 58 条、行为事实 55 条保持不变：

| 表 | 完成后计数 |
|---|---:|
| `iam_user_account` | 2 |
| `iam_login_identifier` | 2 |
| `iam_password_credential` | 2 |
| `iam_user_role_fact` | 4 |
| `iam_auth_session` | 6 |
| `iam_refresh_token` | 7 |
| `iam_action_token` | 2 |
| `iam_security_event` | 12 |
| `user_personalization_consent_fact` | 5 |
| `user_declared_profile` | 3 |
| `user_declared_profile_history` | 3 |
| `recommendation_task` | 58 |
| `user_behavior_event` | 55 |

计划预算为 2 条授权操作、11 条追加事实和 8 个受控状态更新，共 21 个数据库操作；执行报告与前后计数一致。Neo4j、Chroma、推荐业务、反馈业务和 DeepSeek 请求数均为 0。

## 证据与凭据边界

- 主要验收证据：[acceptance.json](/Users/tianyuhang/Documents/RecPro/artifacts/verification/stage2-identity-grant-recovery/stage2-reader-grant-recovery-20260831/acceptance.json)
- 原始执行器生成的历史证据文件保留在 `artifacts/verification/stage2-identity-closure/...`，未覆盖或删除；计划目标路径另行创建了校正副本。
- 测试读者密码保存在仓库外观感知的本地忽略文件 `.env.stage2-reader-login.local`，权限为 `0600`；密码、刷新令牌、激活码和 root 密钥均未进入日志、Git 或证据。
- 文件删除、数据库物理删除、容器删除和数据卷删除均为 0。

## 阶段状态

- 阶段 1：已完成。
- 阶段 2：已完成，真实本地登录与个性化闭环可用。
- 阶段 3：待执行。重点是 Neo4j Community 的独立只读副本与 v1/v2 计数不变验收，需单独 ChangePlan。
- 阶段 4–8：按既定顺序待执行；不将本阶段的合成账号行为当作论文实验数据。
