# LibraMAS 阶段 2：真实身份闭环 successor ChangePlan

```text
记录 ID：LIBRAMAS-STAGE2-IDENTITY-PLAN-20260831-001
生成日期：2026-08-31（Asia/Shanghai）
分支：codex/g1-runnable-skeleton
代码提交：05a2a9ad7f71da03ebb5421c0ce4df3a3b729a0d
当前分支头：99c898edf0e95ac20dc284d6d3ce5fb01e139847
状态：READY_FOR_EXPLICIT_APPROVAL / NOT_APPLIED
```

## 1. 计划身份

- `plan_id`：`34dab63c-b9f1-56e8-8719-d12287530591`
- `plan_hash`：`2a87eb15fca5dcfd2dbac6fc468f2020e2e3c198946574bee560ed1126634cfa`
- 计划文件：[stage2-identity-closure-successor-20260831.json](/Users/tianyuhang/Documents/RecPro/plans/stage2-identity-closure-successor-20260831.json)
- 执行器：[execute_stage2_identity_closure.py](/Users/tianyuhang/Documents/RecPro/scripts/execute_stage2_identity_closure.py)

只有上述 `plan_id` 与 `plan_hash` 同时获得明确批准，执行器才允许从只读门禁进入
`--apply`。计划绑定的功能代码提交为 `05a2a9a`；当前分支头是其后仅增加计划文件的
后代，输入哈希未改变。

## 2. 固定范围

目标数据库固定为 `mysql://127.0.0.1:62306/recpro`，固定测试身份为非真实个人：

- 预期 `user_id=10001`。
- 登录标识：`LIBRAMAS-STAGE2-READER-20260831`。
- 账号显示名：`LibraMAS 阶段二测试读者`。
- 声明画像为研究原型测试值：图书馆学、研究生、多智能体推荐、中文偏好。
- 不复用合成用户 `1001/1002`，不创建真实个人账号。

## 3. 精确预算

| 类别 | 上限/预期 |
|---|---:|
| 追加式数据库行 | 23 |
| 受控当前状态更新操作 | 10 |
| `max_changes` | 33 |
| DeepSeek 请求 | 0 |
| 推荐/反馈/行为事实 | 0 |
| Agent Workspace 审计事实 | 0 |
| Neo4j 写入 | 0 |
| Chroma 写入 | 0 |
| 新容器/数据卷 | 0 |
| 文件删除、数据库记录删除、数据库删除 | 0 |

追加内容包括 1 个账号、1 个登录标识、1 个激活令牌、1 条 `user` 角色事实、1 个
Argon2id 凭据、2 个会话、3 个刷新令牌、6 条安全事件、4 条授权授予事实、1 条
`BEHAVIOR_LEARNING` 撤回事实、1 条声明画像历史事实和 1 条当前画像投影。

受控更新仅涉及登录计数/时间、账号激活、激活令牌消费、刷新令牌消费、会话最近访问和
注销状态；不更新或覆盖历史事实。

## 4. 执行顺序

1. 只读核对 MySQL 基线、`AUTO_INCREMENT=10001`、固定标识未占用和应用 readiness。
2. 在本地 `0600` 且 Git 忽略的 `.env.stage2-reader-login.local` 中生成测试密码；不在
   终端、日志、计划或证据中输出密码。
3. 使用既有管理员凭据登录，创建测试读者并立即消费一次性激活码。
4. 测试读者登录，追加四项授权，撤回行为学习授权并写入声明画像。
5. 执行一次刷新令牌轮换，随后注销测试读者和管理员。
6. 只读回读所有表计数、账号状态、有效授权、画像投影和注销状态，生成不可覆盖的
   `artifacts/verification/stage2-identity-closure/<run_id>/acceptance.json`。

执行器遇到任何前置状态偏差或中途错误都会停止并保留已追加事实，不执行补偿删除或覆盖；
部分状态需要新的 forward-only successor 计划处理。

## 5. 当前未完成

- 用户尚未批准本计划的精确 `plan_id + plan_hash`，因此没有数据库连接写操作。
- 尚未创建测试账号、会话、授权、画像或受保护测试密码文件。
- 尚未进行浏览器推荐、反馈或 DeepSeek 验收；这些属于后续阶段，不能借本计划执行。

计划构建器和执行器的默认模式均为零写入 dry-run；专项测试 4 项通过，身份专项测试
共 41 项通过。当前 Git 工作区干净且计划已推送到远程分支。
