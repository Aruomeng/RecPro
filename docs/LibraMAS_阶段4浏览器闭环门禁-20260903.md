# LibraMAS 阶段 4：真实浏览器闭环门禁（2026-09-03）

## 本次完成

本次基于当前提交 `f0a530da6ea2e2a26171ba7e3b11b5bd2ed55579` 完成了三项工作：

1. 使用真实运行中的前端、后端、MySQL、Neo4j v2 和 Chroma 执行只读基线核对。
2. 修正规则意图回退与 LLM 意图代理的引导式澄清契约，并用精确冷启动输入回归验证。
3. 重新冻结六个浏览器业务场景的 successor ChangePlan，避免继续使用旧提交绑定的计划；同时为 `cold_user_guided` 生成独立的 19 行追加计划并完成批准执行。

只读基线证据：

- `artifacts/verification/g5/stage4-browser-baseline-20260903-005/readonly.json`
- `artifacts/verification/g8/g8-cold-preflight-20260903-004/readonly.json`
- `artifacts/verification/g8/g8-cold-apply-20260903-002/apply.json`

只读核对结果为 `PASS`：健康接口为 `200`，推荐能力可用，MySQL 身份和最小权限检查通过；前后全库计数一致。

## 浏览器只读检查

在本地前端 `http://127.0.0.1:5173/` 上检查了首页、知识图谱、馆藏洞察、系统状态、推荐入口和阅读路径入口：

| 视口 | 结果 |
|---|---|
| 1920×1080 | 无横向溢出；无越界隐藏裁切元素 |
| 3840×2160 | 无横向溢出；无越界隐藏裁切元素 |
| 1366×768 | 无横向溢出；无越界隐藏裁切元素 |
| 1280×720 | 无横向溢出；无越界隐藏裁切元素 |
| 390×844 | 无横向溢出；无越界隐藏裁切元素 |

已确认的交互边界：

- 访客可以读取首页、知识图谱、馆藏洞察和资源探索。
- 访客进入“智能推荐”和“阅读路径”时显示登录弹窗，不发送业务 POST。
- 图谱搜索可读取真实局部子图；无结果时显示明确的 Neo4j 只读状态和重试入口。
- 全局 Agent 工作栏可展开，显示 8 个业务 Agent、Orchestrator 状态、上下文版本、真实事件时间线、建议和数据源状态。
- 浏览器控制台本次错误/警告数为 `0`。

## 当前 successor ChangePlan（六场景总预算）

- `plan_id`：`4fb352ba-aa7f-5cda-87a5-392bc5ee25b5`
- `plan_hash`：`afff89e7584eb2cda5946f059e6b5e141962f39da393eedb69f696b1828676ae`
- 计划文件：`artifacts/verification/g8/stage4-browser-successor-20260903-004/browser-scenario-plan.json`
- 计划校验：`PASS`
- 运行身份：`demo_cold`、`demo_clear`、`demo_topic`、`demo_path`、`demo_negative`、`demo_degraded` 六个隔离场景身份
- 图谱版本：`lib-books-v1-20260810`（浏览器计划只冻结接口场景，不导入或修改图数据）
- 向量版本：`lib-books-vector-v1-20260811`
- 模型配置：DeepSeek `deepseek-v4-flash`

计划冻结的场景与上限：

| 场景 | 预期结果 | MySQL 最大追加 | DeepSeek 最大请求 |
|---|---|---:|---:|
| `cold_user_guided` | `WAITING_CLARIFICATION` | 20（总预算上限） | 0 |
| `clear_user_recommendation` | `COMPLETED` | 56 | 16 |
| `topic_user_explanation` | `COMPLETED` | 56 | 16 |
| `reading_path_clarification` | `COMPLETED` | 56 | 16 |
| `negative_feedback_adjustment` | `COMPLETED` | 62 | 16 |
| `degraded_dependency_path` | `DEGRADED_COMPLETED` | 56 | 16 |
| **合计上限** |  | **306** | **80** |

该文件记录六场景总门禁；总计划自身仍为 `DRY_RUN`，不授权其余场景批量执行。其安全断言为：Outbox 领取为 `0`、Neo4j/Chroma 写入为 `0`、DeepSeek 请求为 `0`、文件/数据库/容器/数据卷删除为 `0`。

## 冷启动场景独立追加计划（已执行）

只读预演已经证明冻结的 `cold_user_guided` 请求会在四个 Agent 后进入 `WAITING_CLARIFICATION`，其精确写入边界为 19 行：任务 1、状态转移 4、策略 1、追踪 1、上下文 1、澄清 1、Agent 消息 4、Agent 结果 4、Agent 产物 1、编排结果 1。

- 只读证据：`artifacts/verification/g8/g8-cold-preflight-20260903-004/readonly.json`
- 独立计划文件：`artifacts/verification/g8/g8-cold-plan-20260903-004/cold-user-guided-change-plan.json`
- `plan_id`：`8c5a11a1-a88a-50ad-abb1-17d4a8d16de8`
- `plan_hash`：`449959448129899eea58dfa94988cc5462565865406b443acdc29be3b14994db`
- 计划校验：`PASS`，当前提交绑定为 `f0a530da6ea2e2a26171ba7e3b11b5bd2ed55579`
- 执行器：`scripts/execute_g8_cold_user_guided_plan.py`

执行证据：`artifacts/verification/g8/g8-cold-apply-20260903-002/apply.json`。已追加 MySQL 19 行，任务 `398900f1-030a-5eb3-bcce-d43980812a09` 处于 `WAITING_CLARIFICATION`；逐表回读与全表计数一致。DeepSeek 0 次、Neo4j/Chroma 0 次、Outbox 0 次、删除 0。

## 下一道门禁

## 清晰查询推荐场景独立追加（已执行）

`clear_user_recommendation` 已根据当前只读基线生成独立精确计划，并在获得明确批准后执行：

- 只读基线：`artifacts/verification/g4/g8-clear-readonly-20260903-001/readonly.json`、`artifacts/verification/g5/stage4-browser-baseline-20260903-006/readonly.json`
- 计划文件：`artifacts/verification/g4/g8-clear-apply-20260903-001/g4-recommendation-projection-change-plan.json`
- `plan_id`：`da359cf9-f54b-5e55-9529-8a148eea1ee9`
- `plan_hash`：`d10d044eb8ba746106810cb4b37c41fba6936261992c4fb820c41c8ff3631987`
- 请求：`请推荐多智能体系统与智慧图书馆相关图书`，`user_id=1001`，`TOPIC_RESOURCES`，上限 8 项
- 任务：`415f41ed-de5e-51d4-81a5-694a5fff888e`，状态 `COMPLETED`，返回 8 项

首次执行已完成事务提交、幂等重放（`201/false` 后 `200/true`）和任务读取；由于计划文件与执行证据意外使用同一目录，最后落盘步骤触发 `FileExistsError`。未重放 POST、未删除目录。随后使用只读恢复核对器验证了同一任务和全量计数，并把不可覆盖证据写入：

`artifacts/verification/g4/g8-clear-recovery-20260903-001/g4-recommendation-projection-recovery.json`

核对结果：MySQL 精确追加 53 行（任务 1、转移 8、候选 9、记录 1、条目 8、解释 8、策略 1、追踪 1、Agent 消息/结果各 7、产物 1、编排结果 1）；DeepSeek Intent 1 次且无回退；Neo4j/Chroma/Outbox 均为 0；文件删除、数据库物理删除均为 0。执行器已修复同目录计划/证据冲突，后续会自动使用 `-apply` 兄弟目录。

下一道门禁为 `topic_user_explanation`。其推荐任务、模型请求或反馈写入必须根据当前提交、最新计数和固定 UUID 重新生成独立精确 ChangePlan，并在执行前获得该场景的 `plan_id + plan_hash` 明确批准；不会复用六场景总计划批量执行。

## 主题解释场景独立追加（已执行）

`topic_user_explanation` 已在提交 `1c6da51228971437315537f55aa3407b431294cc` 上完成真实执行：

- 只读基线：`artifacts/verification/g4/g8-topic-readonly-20260903-002/readonly.json`、`artifacts/verification/g5/stage4-browser-baseline-20260903-008/readonly.json`
- 计划文件：`artifacts/verification/g4/g8-topic-apply-20260903-001/g4-recommendation-projection-change-plan.json`
- `plan_id`：`937c0973-c9c8-5c8c-a554-2a9da6557237`
- `plan_hash`：`7cd54ff2ad7a79192db9095105ab2d280f5de8f61327279bf603102de30dbdfa`
- 请求：`围绕多智能体、知识图谱和智慧图书馆解释推荐理由`，`user_id=1001`，资源类型 `BOOK/PAPER`，`TOPIC_RESOURCES`，上限 8 项
- 任务：`feb99fdb-e91c-53aa-b990-cc34b5210f6e`，状态 `COMPLETED`，记录 `8` 项

执行证据：`artifacts/verification/g4/g8-topic-apply-20260903-001-apply/g4-recommendation-projection-apply.json`；独立只读对账：`artifacts/verification/g4/g8-topic-reconcile-20260903-001/reconciliation.json`。MySQL 精确追加 52 行（任务 1、转移 8、候选 8、记录 1、条目/解释各 8、策略 1、追踪 1、Agent 消息/结果各 7、产物 1、编排结果 1）；Intent DeepSeek 1 次、Explanation DeepSeek 8 次，均无回退且证据引用校验通过。Neo4j/Chroma/Outbox 均为 0，重放为 `201/false` 后 `200/true` 且零新增，删除为 0。

目前阶段 4 已完成 `cold_user_guided`、`clear_user_recommendation`、`topic_user_explanation` 三个场景；下一道门禁为 `reading_path_clarification`，必须基于最新提交和最新计数重新生成独立精确 ChangePlan 并单独批准。

## 阅读路径场景独立追加（已执行）

`reading_path_clarification` 已在提交 `343f581068eeef03a97527577bd20f426cce818e` 上完成真实执行：

- 只读基线：`artifacts/verification/g4/g8-path-readonly-20260903-001/readonly.json`、`artifacts/verification/g5/stage4-browser-baseline-20260903-009/readonly.json`
- 计划文件：`artifacts/verification/g4/g8-path-apply-20260903-001/g4-recommendation-projection-change-plan.json`
- `plan_id`：`cc817dcb-bb20-54d7-af9c-a5e77a1c988a`
- `plan_hash`：`50e2982a86d4bd07126cb9fb05ec389c29e19d77fa8524870ea3aaa45452b9d0`
- 请求：`请给我一条从推荐系统基础到多智能体智慧图书馆的学习路径`，`user_id=1001`，资源类型 `BOOK/PAPER`，输出 `READING_PATH`，上限 8 项
- 任务：`b922fcb9-d97a-50b4-8dfe-b966137befd4`，状态 `COMPLETED`，记录 8 项

执行证据：`artifacts/verification/g4/g8-path-apply-20260903-001-apply/g4-recommendation-projection-apply.json`；独立只读对账：`artifacts/verification/g4/g8-path-reconcile-20260903-001/reconciliation.json`。MySQL 精确追加 52 行（任务 1、转移 8、候选 8、记录 1、条目/解释各 8、策略 1、追踪 1、Agent 消息/结果各 7、产物 1、编排结果 1）；Intent DeepSeek 1 次、Explanation DeepSeek 8 次，均无回退且证据引用校验通过。Neo4j/Chroma/Outbox 均为 0，重放为 `201/false` 后 `200/true` 且零新增，删除为 0。

目前阶段 4 已完成 `cold_user_guided`、`clear_user_recommendation`、`topic_user_explanation`、`reading_path_clarification` 四个场景；剩余 `negative_feedback_adjustment` 与 `degraded_dependency_path`，必须基于最新提交和最新计数分别生成独立精确 ChangePlan 并单独批准。

本阶段没有删除、覆盖或修改任何历史文件、数据库事实、容器或数据卷。
