# LibraMAS 阶段 4：真实浏览器闭环门禁（2026-09-03）

## 本次完成

本次基于当前提交 `704696539aec9cfe7c996d1cfa75f32bbe183af8` 完成了三项不产生业务副作用的工作：

1. 使用真实运行中的前端、后端、MySQL、Neo4j v2 和 Chroma 执行只读基线核对。
2. 修正规则意图回退与 LLM 意图代理的引导式澄清契约，并用精确冷启动输入回归验证。
3. 重新冻结六个浏览器业务场景的 successor ChangePlan，避免继续使用旧提交绑定的计划；同时为 `cold_user_guided` 生成独立的 19 行追加计划。

只读基线证据：

- `artifacts/verification/g5/stage4-browser-baseline-20260903-003/readonly.json`
- `artifacts/verification/g8/g8-cold-preflight-20260903-002/readonly.json`

只读核对结果为 `PASS`：健康接口为 `200`，推荐能力可用，MySQL 身份和最小权限检查通过；前后全库计数一致。

## 浏览器只读检查

在本地前端 `http://127.0.0.1:5173/` 上检查了首页、知识图谱、馆藏洞察、系统状态、推荐入口和阅读路径入口：

| 视口 | 结果 |
|---|---|
| 1920×1080 | 无横向溢出；无越界隐藏裁切元素 |
| 3840×2160 | 无横向溢出；无越界隐藏裁切元素 |
| 1366×768 | 无横向溢出；无越界隐藏裁切元素 |
| 1280×720 | 无横向溢出；无越界隐藏裁切元素 |
| 390×844 | 无横向溢出；无越界隐藏裁切元素 |

已确认的交互边界：

- 访客可以读取首页、知识图谱、馆藏洞察和资源探索。
- 访客进入“智能推荐”和“阅读路径”时显示登录弹窗，不发送业务 POST。
- 图谱搜索可读取真实局部子图；无结果时显示明确的 Neo4j 只读状态和重试入口。
- 全局 Agent 工作栏可展开，显示 8 个业务 Agent、Orchestrator 状态、上下文版本、真实事件时间线、建议和数据源状态。
- 浏览器控制台本次错误/警告数为 `0`。

## 当前 successor ChangePlan（六场景总预算）

- `plan_id`：`15fe8aea-8535-506a-8d9e-11c39b7bb95a`
- `plan_hash`：`0edd91627847b772e33094c71ab335cbde6457eccc51c25f54fb288b1d29f523`
- 计划文件：`artifacts/verification/g8/stage4-browser-successor-20260903-002/browser-scenario-plan.json`
- 计划校验：`PASS`
- 运行身份：`demo_cold`、`demo_clear`、`demo_topic`、`demo_path`、`demo_negative`、`demo_degraded` 六个隔离场景身份
- 图谱版本：`lib-books-v1-20260810`（浏览器计划只冻结接口场景，不导入或修改图数据）
- 向量版本：`lib-books-vector-v1-20260811`
- 模型配置：DeepSeek `deepseek-v4-flash`

计划冻结的场景与上限：

| 场景 | 预期结果 | MySQL 最大追加 | DeepSeek 最大请求 |
|---|---|---:|---:|
| `cold_user_guided` | `WAITING_CLARIFICATION` | 20（总预算上限） | 0 |
| `clear_user_recommendation` | `COMPLETED` | 56 | 16 |
| `topic_user_explanation` | `COMPLETED` | 56 | 16 |
| `reading_path_clarification` | `COMPLETED` | 56 | 16 |
| `negative_feedback_adjustment` | `COMPLETED` | 62 | 16 |
| `degraded_dependency_path` | `DEGRADED_COMPLETED` | 56 | 16 |
| **合计上限** |  | **306** | **80** |

该文件只记录门禁和计划，不代表六个场景已经执行。计划自身仍为 `DRY_RUN`，其安全断言为：业务写入未授权、Outbox 领取为 `0`、Neo4j/Chroma 写入为 `0`、DeepSeek 请求为 `0`、文件/数据库/容器/数据卷删除为 `0`。

## 冷启动场景独立追加计划

只读预演已经证明冻结的 `cold_user_guided` 请求会在四个 Agent 后进入 `WAITING_CLARIFICATION`，其精确写入边界为 19 行：任务 1、状态转移 4、策略 1、追踪 1、上下文 1、澄清 1、Agent 消息 4、Agent 结果 4、Agent 产物 1、编排结果 1。

- 只读证据：`artifacts/verification/g8/g8-cold-preflight-20260903-002/readonly.json`
- 独立计划文件：`artifacts/verification/g8/g8-cold-plan-20260903-002/cold-user-guided-change-plan.json`
- `plan_id`：`2b9ce806-e48e-596c-9c91-66749501f376`
- `plan_hash`：`9678b948130113370b226e59df57debc9f143949702ebdf60e41c8f08d401e86`
- 计划校验：`PASS`，当前提交绑定为 `704696539aec9cfe7c996d1cfa75f32bbe183af8`
- 执行器：`scripts/execute_g8_cold_user_guided_plan.py`

该独立计划仍是 `DRY_RUN`，只允许在再次明确批准完全一致的 `plan_id + plan_hash` 后执行。执行上限为 MySQL 19 行、DeepSeek 0 次、Neo4j/Chroma 0 次、Outbox 0 次；不会产生删除或覆盖。

## 下一道门禁

六个场景中凡是会创建推荐任务、写入反馈/行为、领取 Outbox 或调用 DeepSeek 的部分，必须根据本 successor 的固定 UUID、当前计数和当前代码重新生成对应的独立精确 ChangePlan，并在执行前获得该场景的 `plan_id + plan_hash` 明确批准。当前未收到冷启动独立计划的精确批准，因此尚未发送业务 POST、登录新会话或调用模型。

本阶段没有删除、覆盖或修改任何历史文件、数据库事实、容器或数据卷。
