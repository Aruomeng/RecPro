# 阶段 4：阅读路径与反馈学习执行记录（2026-08-31）

## 阅读路径

- 已批准 ChangePlan：`24f63ace-cf49-50a2-a7e5-6f0bd0eb1a62`。
- Plan hash：`7b764f3abe525ba28fb18a930037e756667b10d58dfd543c28bad1bc5839bd8e`。
- 任务：`394003c8-7fa1-59b3-ac61-aae503121f7b`；推荐记录：`57`；状态：`COMPLETED`。
- 输入：`推荐系统与智慧图书馆`；输出类型：`READING_PATH`；结果数：8。
- 真实读取的数据面：MySQL、最终 Neo4j v2 只读副本 `lib-books-v2-20260828` 与版本化 Chroma。首次投影前的只读预演完成 7 个核心 Agent，三个召回通道均可用。

### 追加对账

阅读路径仅向 MySQL 追加 52 行：1 个任务、8 个状态转换、8 个候选、1 条推荐记录、8 个推荐项、8 条解释、1 个策略、1 条 Trace、7 条 Agent 消息、7 条 Agent 结果、1 个 Agent Artifact 和 1 个 Orchestration Result。

- 首次请求：`201`；相同幂等键重放：`200`，新增行数为 0。
- DeepSeek、Neo4j 与 Chroma 写入均为 0。
- 无物理删除、文件删除、覆盖、迁移或种子操作。

## 遗留 Outbox 精确恢复

反馈链启动前检查发现历史 `profile_update_outbox #49` 为 `PENDING`。为防止新的 Worker 误消费旧事实，先执行了独立的精确白名单计划：

- ChangePlan：`4ad32644-b7e5-52bd-8f7f-4ed2863a15dc`。
- 仅允许 Worker 领取 #49；它关联历史行为事件 #84。
- #49 已进入 `DONE`，画像版本从 34 推进至 35，第二次 Worker 运行领取 0 条。
- 未触碰非目标 Outbox，未删除或改写原始行为事件。

## 反馈学习闭环

- 已批准 ChangePlan：`5bfc3287-7061-5140-ac6e-c13c523cce5e`。
- Plan hash：`72e77c9f56f5b350602ee0ed390dd08bc9cbc649e9dafaf34cc916635c6a1920`。
- 目标：阅读路径首项 `recommendation_item=369`、资源 `5953`《从零开始构建企业级推荐系统》、演示画像用户 `1001`。
- 追加 1 条有效曝光、1 条 `NOT_INTERESTED/TOPIC_NOT_INTERESTED` 反馈、1 条点击行为，并精确消费 2 条新 Outbox（#50、#51）。

### 结果与安全边界

- 执行状态：`PASS`；同 UUID 的曝光、反馈和行为重放均为幂等重放。
- 两条 Outbox 都为 `DONE`，每条仅尝试一次；第二次 Worker 运行领取 0 条。
- 画像重放版本：36、37；目标资源新增 `HIDDEN` 抑制状态，来源为反馈事件 #86。
- 逐表新增严格等于计划上限 32 行；非目标表变化为 0。
- DeepSeek、Neo4j、Chroma、外部网络调用、物理删除、文件删除及覆盖均为 0。

执行中的唯一键重复提示来自同 UUID 重放与历史画像事实的去重路径；独立只读对账已验证所有预期事实唯一、Outbox 已完成，且不存在计划外增量。

## 证据位置

- `artifacts/verification/g4/stage4-reading-path-readonly-20260831-001/readonly.json`
- `artifacts/verification/g4/stage4-reading-path-append-20260831-001/g4-recommendation-projection-change-plan.json`
- `artifacts/verification/g4/stage4-reading-path-append-20260831-001-apply/g4-recommendation-projection-apply.json`
- `artifacts/verification/g4/stage4-reading-path-append-20260831-001-reconcile-001/reconciliation.json`
- `artifacts/verification/g5/stage4-outbox49-recovery-20260831-001/g5-existing-outbox-change-plan.json`
- `artifacts/verification/g5/stage4-outbox49-recovery-20260831-001-apply/g5-existing-outbox-apply.json`
- `artifacts/verification/g5/stage4-feedback-outbox-append-20260831-002/g5-feedback-worker-change-plan.json`
- `artifacts/verification/g5/stage4-feedback-outbox-append-20260831-002-apply/g5-feedback-worker-apply.json`
- `artifacts/verification/g5/stage4-feedback-outbox-append-20260831-002-reconcile-001/reconciliation.json`

## 阶段 5 起点

阶段 5 已保持为不调用模型的 Fixture 运行时：后台规划由真实 Workspace observation 触发，限制为每会话最多 3 次、间隔至少 10 分钟、每设备每日最多 12 次；只允许返回白名单 UI Directive。真实 DeepSeek 后台规划仍关闭，必须先取得单独的模型预算 ChangePlan。
