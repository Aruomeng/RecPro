# LibraMAS 阶段 6：开发代理实验执行记录

执行日期：2026-09-01  
执行结论：`PASS`（仅 `DEVELOPMENT_PROXY` 开发性验证，不是确认性论文实验）。

## 本次完成

- 以已授权的本地 `Lib/` 馆藏生成不可覆盖的确定性代理评价切分。
- 执行 B0、B1、B2、B3、Proposed、`A_ONE_HOP_KG` 及全部既定消融；执行 8 类故障注入矩阵。
- 生成冻结预测、指标、Markdown 报告、CSV 表与 SVG 图。
- 运行标准库回归测试，验证确定性、公平性和不可覆盖规则。

## 冻结证据

| 项目 | 值 |
| --- | --- |
| 数据集版本 | `lib-development-proxy-20260901-v3` |
| 资源数 / 代理任务数 | 14,571 / 105 |
| train / validation / test | 61 / 20 / 24 |
| 主预测 / 故障预测 | 336 / 64 |
| 主预测 SHA-256 | `060157509c92b43bb8efe4bc512fa159b8fa24751fc84dba18ff08adb53252b2` |
| 运行 ID | `libra-dev-suite-20260901-v3` |
| 降级成功率 / Trace 完整率 | 1.0 / 1.0 |
| 数据库读写 / DeepSeek 请求 / 删除 | 0 / 0 / 0 |

本地不可覆盖产物位于：

- `artifacts/verification/experiment-splits/libra-dev-proxy-20260901-001/`
- `experiments/runs/libra-dev-suite-20260901-v3/`

它们由 `.gitignore` 排除，防止把派生产物或本地数据误提交；运行目录中的 `report.md` 保留 `DEVELOPMENT_PROXY` 标识。

## 关键机制检查

- B2 与 Proposed 候选及分数一致：通过。
- B3 与 Proposed 候选及分数一致：通过。
- B3/Proposed 在故障注入中的工具与超时契约一致：通过。
- `A_NO_POLICY` 的策略 Macro-F1 为 0.4894，而 Proposed 为 1.0，说明策略模块差异被运行器检出。
- `A_NO_DIVERSITY` 的列表内主题多样性为 0.1208，低于 Proposed 的 0.2208，说明多样性消融被运行器检出。

上述数值由 Topic、Category、SubjectCode 代理判断产生，只能说明实验运行器能稳定检出机制差异；不能解释为真实读者偏好、真实借阅效果或论文推荐准确率提升。

## 尚未完成与阻断条件

正式论文确认性实验仍处于 `FORMAL_INPUT_BLOCKED`，因为尚未收到：

1. 经许可且脱敏的正式评价数据；
2. License/Permission Manifest；
3. 至少两名独立标注者的盲标注、一致性和仲裁材料；
4. 冻结的 train/validation/test Split；
5. 绑定四类输入、依赖锁与版本的 FROZEN Config Manifest。

材料字段、放置路径和只读门禁命令见 [正式论文实验材料交接清单](LibraMAS_正式论文实验材料交接清单-20260901.md)。在门禁返回 `READY_FOR_FORMAL_RUN` 前，任何后续运行必须继续标记为 `DEVELOPMENT_PROXY`。
